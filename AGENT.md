# AGENT.md — منصة منسج
## مصدر الحقيقة الوحيد · وثيقة التسليم الكاملة بين الجلسات

> **هذا الملف هو نقطة الدخول الأولى لأي جلسة عمل على المشروع.** يُقرأ كاملًا قبل أي تعديل، ويُحدَّث في نهاية كل جلسة. المنهج: وثيقة حيّة تسبق الكود — لا عمل بلا مواصفة، ولا مواصفة بلا تحديث بعد العمل.

---

## 0. الخلاصة — ما هذا المشروع؟

**منسج (Minsaj)** منصة عمل موحّدة للذكاء الاصطناعي، **عربية أولًا** (RTL أولًا، ثم LTR)، تساعد المستخدم على الانتقال من النية إلى نتيجة منظمة قابلة للفهم والحفظ والاستئناف. الاستعارة: أفكار المستخدم خيوط والمنصة نول.

- **الحالة:** أساس واجهة وتجربة كامل على **طبقة بيانات تجريبية** (`packages/mock-api`) — لا خدمات خارجية حقيقية بعد.
- **الخدمات (7+1):** اسأل · تعلّم · ابحث · أنشئ · برمج · حلّل · استكشف + مكتبتي.
- **ثلاث مساحات منجزة نطاقيًا** (تعلّم/ابحث/أنشئ) و**أربع بوابات نموذجية** (اسأل/برمج/حلّل/استكشف).
- **الترتيب المنتجي الحاكم:** النية → مساحة العمل → السياق → التنفيذ → مخرج منظم → الحفظ → الاستئناف.
- **القاعدة الذهبية للجودة:** المنطق قبل الزينة — كل زر له أثر ملموس، أو يُحذف.

## 1. الوصول والبيانات السرية 🔑

| البند | القيمة |
|---|---|
| المستودع | `github.com/zoih6/minsaj-ai-platform` — **عام** |
| الفرع الإنتاجي | `main` فقط |
| النشر | Vercel — مشروع `minsaj-ai-platform` · فريق `4zobir89-lab` · نشر تلقائي من main |
| الإنتاج | `minsaj-ai-platform-4zobir89-labs-projects.vercel.app` |
| متغيرات البيئة | **لا يوجد أي متطلب حاليًا** — التطبيق مغلق على mock |

> 🔴 **قاعدة الأسرار:** المستودع عام — **يُمنع منعًا باتًا** كتابة أي توكن أو مفتاح أو كلمة مرور في أي ملف يُدفع. بيانات الاعتماد (GitHub PAT / Vercel Token) يقدمها المالك **داخل الجلسة** فقط، وتُستخدم من ذاكرة الجلسة وتُنسى — ولا تُخزن في أي ملف.

## 2. البنية التقنية

| البند | القيمة |
|---|---|
| الإطار | Next.js 16 (App Router) + React 19 + TypeScript 5 |
| الأنماط | `src/app/[locale]/...` — كل شيء تحت مسار اللغة (ar/en) |
| الحزم | مساحة عمل npm: `packages/contracts` (Zod) · `i18n` · `mock-api` · `ui` |
| الهوية البصرية | CSS معماري: `src/app/universal.css` يستورد 14 طبقة من `src/app/styles/universal/` |
| الأيقونات | lucide-react 1.44.0 (المصدر الوحيد) |
| Node | 24.x (يطابق Vercel) |

**الأوامر:**

```bash
npm ci                  # تثبيت حتمي
npm run dev             # localhost:3000
npm run build           # يجب أن ينجح بكل المسارات (~29)
npm run lint            # نظيف قبل الدفع
npx tsc --noEmit        # أخطاء foundation-harness المعروفة وحدها مقبولة
```

## 3. خريطة الملفات الأساسية

```text
src/
├── app/
│   ├── [locale]/
│   │   ├── (marketing)/page.tsx        # صفحة الهبوط
│   │   ├── app/
│   │   │   ├── layout.tsx              # الغلاف (App Shell) لكل /app/*
│   │   │   ├── home/                   # «لك» الرئيسية
│   │   │   ├── chat/                   # اسأل — البوابة المشتركة (الاصطلاح: الهوية ask · المسار /chat)
│   │   │   ├── [service]/page.tsx      # 6 خدمات + library (بوابة السجل)
│   │   │   ├── learn|research|create/  # مساحات نطاقية (domain)
│   │   │   ├── agents|flows/…          # أدوات متقدمة (+new/+edit)
│   │   │   ├── models/                 # كتالوج النماذج (+[modelId] ديناميكي + routing)
│   │   │   ├── runs/                   # في مجموعة «التشغيل» (KI-1 مغلق)
│   │   │   └── usage|billing|team/     # في مجموعة «التشغيل» · skills/tools عبر تبويبات الكتالوج
│   │   └── preview/                    # أسطح تحقق معزولة
│   ├── universal.css                   # مدخل CSS المعماري
│   └── styles/universal/*.css          # 14 طبقة (foundations/shell/workspaces…)
├── components/
│   ├── app-shell/app-shell.tsx         # الغلاف + مصفوفات التنقل الأربع
│   ├── universal/service-workspace.tsx # بوابة الخدمة (الأقسام التسعة)
│   ├── domain/                         # مساحات نطاقية + Ops + prototypes
│   └── marketing/site-hero.tsx         # بطل الهبوط
├── features/service-workbench/service-registry.ts   # سجل الخدمة الحاكم
└── lib/universal-content.ts            # بيانات الخدمات (eyebrow/starters/outputTitle)
```

**جدول URL → ملف:** انظر [docs/02-experience/information-architecture.md §3](./docs/02-experience/information-architecture.md) — الجدول الكامل الـ34 مسارًا.

## 4. القرارات المعمارية غير القابلة للكسر

1. **سجل الخدمة** هو نقطة قلب أي مساحة من نموذجية إلى نطاقية — لا تُبنى مساحة خارج السجل.
2. **اتجاه الاعتماد صاعد:** مسارات → سجل → مكونات → حزم. الحزم لا تستورد من src.
3. **CSS بالطبقات فقط:** لا CSS مضمن في المكونات؛ القيم الجديدة تدخل `styles/universal/*`.
4. **i18n إلزامي:** كل نص عبر قواميس `packages/i18n` — لا نص حرفي في JSX (الاستثناءات الموثقةة في domain prototypes تُرحل تدريجيًا).
5. **mock-api مصدر البيانات الوحيد** — لا عملاء شبكة قبل قرار المالك.
6. **قانون الأسرار §1.**

## 5. الطبقات الوظيفية الحالية

| الطبقة | المكونات | الحالة |
|---|---|---|
| الغلاف | app-shell + لوحة أوامر ⌘K (19 وجهة) + تنبيهات | مستقرة |
| البوابة النموذجية | service-workspace (9 أقسام) لاسأل/برمج/حلّل/استكشف | بصرها مكتمل — **منطق الاختيار معلّق (KI-3)** |
| المساحات النطاقية | learn/research/create | منجزة |
| الأدوات المتقدمة | projects/agents/flows/knowledge/models | منجزة (mock) |
| طبقة التشغيل | runs/usage/billing/team/skills/tools | منجزة (mock) — **في التنقل (KI-1 مغلق)** |
| المكتبة | library + شريط «من مكتبتك» | منجزة (حفظ محلي) |

## 6. ما يعمل (مفحوص حاليًا)

- البناء الإنتاجي ينجح بكل المسارات (~29 route) والنشر الإنتاجي `READY`.
- 16 مسارًا رئيسيًا يرجع 200 على الإنتاج (عربي/إنجليزي، الخدمات السبع، المكتبة، الإدارة).
- البوابة النموذجية: آلة حالات كاملة (idle→composing→working→ready) + بدايات سريعة تعبّئ الملقّن.
- ثيمان + اتجاهان بلا تمرير أفقي عند 390/768/1440/1920.
- لوحة الأوامر والتنقّل رباعي المجموعات (19 وجهة) — طبقة التشغيل داخل التنقل (KI-1 مغلق).

## 7. المشاكل المعروفة — اقرأ قبل أي عمل

القائمة الكاملة بدرجات الخطورة وبروتوكول العيب الجديد في [docs/05-process/known-issues.md](./docs/05-process/known-issues.md). الرؤوس:

- **KI-1** 🔴 → **مغلق (2026-09-21):** طبقة التشغيل دخلت التنقل (مجموعة رابعة «التشغيل»).
- **KI-2** 🟠 مجلد `models/[modelId]` معطوب الاسم.
- **KI-3** 🔴 أدوات العمل وأسلوب التنفيذ شكلية — المنطق غير مربوط.
- **KI-4** 🟡 اصطلاح الهوية `ask` مقابل المسار `/chat`.
- **KI-5** 🟡 الحفظ محلي بالجلسة (مقصود في مرحلة الأساس).
- **KI-6** 🟢 أخطاء tsc معروفة معفاة في أسطح المعاينة.

## 8. سير العمل النموذجي لمهمة جديدة

1. **اقرأ:** هذا الملف + ONBOARDING + known-issues + وثيقة نطاق مهمتك من `docs/` (الفهرس: `docs/00-INDEX.md`).
2. **اقرأ سجل العمل:** `worklog.md` — ملاحظات كل جلسة سابقة.
3. **شغّل محليًا:** `npm run dev` → localhost:3000 → تفقد صفحات نطاق مهمتك.
4. **خطط:** حدد المتطلبات المتأثرة من `requirements-app.md` والوثيقة الحاكمة للنطاق.
5. **نفّذ:** تفضيل تعديل الملفات القائمة؛ الملف الجديد يحتاج موضعًا طبقيًا صحيحًا (§3) وذكرًا في الوثائق.
6. **بوابات الجودة (بالترتيب):** `npm run build` نجاح → `npm run lint` نظيف → `tsc` بلا أخطاء جديدة → فحص بصري بالمتصفح (ثيمان × اتجاهان + 390px) → تحديث الوثائق المتأثرة **في الكوميت نفسه**.
7. **احفظ:** كوميت ب convention (`feat|fix|docs|refactor|polish(scope): وصف`) يشير إلى معرّفات FR/KI عند وجودها.
8. **ادفع:** `git push origin main` (❌ ممنوع --force).
9. **تحقق من الإنتاج:** دقيقتان → افتح URL الإنتاج → فحص مسارات التأثر → HTTP 200.
10. **وثّق:** سطر CHANGELOG + قسم في worklog.md (Task ID · Agent · Task · Work Log · Stage Summary).

## 9. بروتوكول فشل النشر

| العرَض | الإجراء |
|---|---|
| فشل بناء على Vercel | اقرأ سجل البناء → أصلح محليًا مع بناء ناجح → دفعة إصلاح — لا revert إلا إذا استعصى خلال محاولتين |
| الموقع يرجع خطأ بعد نشر ناجح | أعد النشر من كوميت سليم سابق (revert) فورًا ثم شخّص بهدوء |
| كسر بصري إنتاجي | أرجِعه بـ revert — «الإنتاج لا يبقى معطوبًا أثناء التشخيص» |
| حذف ملفات | 🔴 ممنوع بدون تأكيد المالك أو توثيق كامل في CHANGELOG |

## 10. جهات الاتصال والتقسيم

- **مالك المنتج:** المستخدم (عربي أولًا — كل الحوار بالعربية).
- **التقسيم المرجعي للجلسة القادمة:** Phase W-1 (ربط منطق الاختيار) — التفصيل في [docs/05-process/roadmap.md](./docs/05-process/roadmap.md).

---

> **الوصية الختامية:** هذا المشروع يُدار بالوثائق لا بالذاكرة. إن وجدت نفسك تعمل بلا مرجع وثائقي لمهمتك — توقف واكتب الوثيقة أولًا. استمرارية الجودة بين الجلسات تعني: كل جلسة تبدأ من هنا وتنتهي بتحديث هنا.
