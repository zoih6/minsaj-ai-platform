# AGENT.md — منصة منسج
## مصدر الحقيقة الوحيد · وثيقة التسليم الكاملة بين الجلسات

> **هذا الملف هو نقطة الدخول الأولى لأي جلسة عمل على المشروع.** يُقرأ كاملًا قبل أي تعديل، ويُحدَّث في نهاية كل جلسة. المنهج: وثيقة حيّة تسبق الكود — لا عمل بلا مواصفة، ولا مواصفة بلا تحديث بعد العمل.

---

## 0. الخلاصة — ما هذا المشروع؟

**منسج (Minsaj)** منصة عمل موحّدة للذكاء الاصطناعي، **عربية أولًا** (RTL أولًا، ثم LTR)، تساعد المستخدم على الانتقال من النية إلى نتيجة منظمة قابلة للفهم والحفظ والاستئناف. الاستعارة: أفكار المستخدم خيوط والمنصة نول.

- **الحالة:** أساس واجهة وتجربة كامل على **طبقة بيانات تجريبية** (`packages/mock-api`) — لا خدمات خارجية حقيقية بعد. **W-3 منجزة (2026-09-21).** **W-DS معتمدة (2026-09-22):** Phase 0 (الحراس) منجزة، **Phase 1 (الأسس) منفذة ومقاسة وبوابة خروجها مُعتمدة (§7.3)، وPhase 2 (السطوح) منفذة ومقاسة (2026-09-22)**: **SUR-01 (P0) محلول** — maxDepth>1 من 10 خلايا ← 0/100 (هوم 3←1) وحاويات-في-حاويات بدرجة البطاقة 10←0 لكل عرض عبر مسبار before/after، و47 قاعدة داخلية سُطّحت إلى مجموعات mj-surface--flat · **HIE-02 محلول** — الإعدادات على mj-section/mj-surface/mj-data-list (نصف 16/12 حسب الخريطة، صفر بطاقات بلا ظل، صفر فئات settings-*) · **HIE-03 محلول** — المعرفة/الوكلاء/التدفقات/التشغيلات/الفريق/المشاريع صفوف مقسمة في حاوية واحدة (بطاقات 9←6 · 10←8 · 10←8 · 7←5 @390). القياسات في `evidence/baselines/phase2/` — سجل انحراف كامل (198 فرقًا كلها مصنفة، صفر انحدارات tapUnder44). **بوابة خروج Phase 2 مُعتمدة (§7.4) وPhase 3 (الكروم) منفذة ومقاسة في اليوم نفسه: NAV-01..05 محلولة** — رأس صف مفرد 56px شبه مصمت (0.95) يعلن علامة المساحة فقط (107/65px ← 56px على كل مسار، صفر صفوف ثانية، صفر تكرار هوية) · عامل إرساء عائم واحد (لهجتا الشريط/الدوك 2 ← 1: إنست 16px · نصف 16 · ظل توكن واحد + خط شعرة واحد) · هرمية درج T1–T4 (T1 13px/600/48px + حبة أصلية ومؤشر 3px · T2/T3/T4 13.5px/500/44px بصبغة ناعمة · «مساحتي» مسمّاة ومثبتة خارج التمرير · طيّ «التشغيل» على الجوال) · الميزانية 15.5–21.2% ← 14.5% موحدًا @390 و13.1% @430. القياسات في `evidence/baselines/phase3/` (401 فرق كروم كلها مصنفة · صفر تمرير أفقي ×100 + 360/375/980 · صفر انحدارات لمس). **بوابة خروج Phase 3 مُعتمدة باعتماد مشروط (2026-09-22، §7.5): المالك أقر بتحقق أهداف NAV-01..05 واشترط للإغلاق النهائي توثيق اختبار عدم حجب الـ Mobile Dock لآخر محتوى قابل للتفاعل — الشرط وُفّي ووُثّق في الجلسة نفسها (98/98 خلية PASS، `evidence/baselines/phase3/dock-occlusion-probe.json`)، واختلافات أنماط المحتوى الداخلي تبقى ضمن نطاق Phase 4 لا عيبًا في Phase 3. Phase 4 (الأنماط) منفذة ومقاسة في الجلسة نفسها: INT-01 محلول** — البوابة composer-first وقالب §2.3: كتلة عنوان L2 (حوافل + h1 + سطر تعريف) ← بطاقة الملحن أولًا (ملقّن ينمو + حبة تنفيذ واحدة فوق الطية @390 — كانت تحتها) + شريط تكوين بخلايا «الأسلوب/الأداة» يفتح إفصاحًا تدريجيًا واحدًا في كل مرة (النمط مقسّم افتراضيًا سريع · الأداة القائدة تنفتح بعد أول ضغطة حرف مرة واحدة بمزلاج · خيارات متقدمة وراء إفصاحها) + شريط تقدم رباعي رفيع داخل البطاقة أثناء التنفيذ + سطر الثقة في تذييلها + بدايات سريعة كروابط مجردة (44px أرضية) — القياس: أقسام 8←3 · بطاقات 18←7 · ارتفاع الصفحة 2214←1182px @390 · صفر فروق في الخلايا الـ95 الأخرى. **INT-02 محلول مع تصحيح تشخيص:** قاعدة shell.css بـ480ms كانت ميتة في التتابع (motion.css المستورد أخيرًا يملك الحركة الفعلية: 200ms تلاشٍ بلا إزاحة) — الإصلاح: مالك واحد في motion.css بالقيم المعتمدة **240ms + إزاحة 4px** مقيسة حية عبر CSSOM، والقاعدة الميتة حُذفت بإحالة. القياسات في `evidence/baselines/phase4/` (شرط §7.5 أعيد التحقق عليه بعد التغيير: 98/98). **بوابة خروج Phase 4 مُعتمدة (2026-09-23، §7.6) وPhase 5 (العلامة والشبكة) منفذة ومقاسة في الجلسة نفسها: VIS-01 محلول** — جسر توكنز التسويق: كل قيم ألوان `@theme` السبعة مراجع `var(--u-*)` (brand→--u-primary · light→strong · dark→deep · cyan→--u-cyan · emerald→--u-mint · emerald-ink→--u-mint-ink · pink→--u-pink) والهكسات المستقلة حُذفت (brand-deep/brand-purple ماتا: التحويم عبر خطوة السطوع المن platformية وذيل التدرج على --u-cyan) وتدرج البطل/ألوان التحديد/الوهجين أعيد تعبيرها بالتوكنات وحذف قاعدة إعادة ربط الزمرد الداكن — القياس: 8/8 هكس خام ← 0 · تباين الهبوط نظيف بعد إصلاح حبر الزمرد داخل الطور · **VIS-03 محلول** — طيّ قماش التركيز: نطاق `--fc-` (32 إعلانًا حيًا) مات بالكامل، واللوحة الثانية مُعلنة كتوكن `--u-canvas-focus` (#f4f5fc/#030712) تحت `[data-canvas="focus"]` على المسارات الأربعة، و`--sp-bg` يرتبط بالتوكن نفسه (مالك واحد، الرسم مطابق بالبكسل) · **GRD-01 محلول** — سلم الحاويات: إحصاء القيم المعلنة عبر CSSOM من 21 قيمة مميزة ← السلم فقط narrow 560 / prose 780 / wide 1040 / full 1440 (حاوية الصفحة 1240/1320/1520/1536 ← 1440 · القراءة 600–830 ← 780 · الملحن 840 ← 560 · الأسطح 920/980 ← 1040) وحذف --mj-content-max/--u-content-max/--u-reading-max، مع توثيق حدّ الحوارات ومحفزات CQ خارج النطاق. القياسات في `evidence/baselines/phase5/` (صفر فروق حقول ×100 خلية). **بوابة خروج Phase 5 مُعتمدة (2026-09-24، §7.7) وPhase 6 (تقسية QA) منفذة ومقاسة في اليوم نفسه: G-7 في وضع الخطأ** — نصف stylelint بـ ratchet ‏`--max-warnings 929` والنصف المخصص بـ `--enforce` (أصفار مطلقة: sub-10px وunpaired tracking وأصناف مكونات جديدة · راتشات census شاملة وراتشات per-file تمنع تهريب الدين بين الملفات — مُختبَر بانحدار محقون قُبض عليه على المحاور الخمسة) · **مسبار G-6 حي في CI** (`scripts/visual-qa/shell-probe.mjs`: ‏390/430/980-coarse + ضوابط 1024/1440 الدقيقة — قبل: 3/3 مسارات تخفق عند 980 كما تنبأ RES-01 · بعد: 11/11 PASS) · **مصفوفة الاعتماد §5 حية** (`scripts/visual-qa/signoff.mjs` + `docs/05-process/SIGN-OFF-LOG.md` — CI يرفض دفعات فئات المالك بلا قيد مؤرخ في نفس الدفعة) · **RES-01 محلول** (حارس المؤشر R-RES-1b في useViewportMode + محرك عدم التصغير R-RES-1a: طبقة coarse-wide تضرب السلالم ×2.37 عند (coarse ∧ ≥768) — nav ‏5.49→13.0px فعلي · متن 6.76→16.0 · مونو 5.91→12.5 · الدرج يفتح · الميزانية 14.3% · صفر انحراف التقاط ×100 مقابل phase5 · ضوابط المؤشر الدقيق تثبت صفر تسرب). القياسات في `evidence/baselines/phase6/`. **بانتظار المالك: اعتماد بوابة خروج Phase 6 — وهي أيضًا بوابة خروج W-DS كلها وبقرارها يُفتح W-4. لا مشكلة تُعلن محلولة قبل Gate A+B.**
- **الخدمات (7+1):** اسأل · تعلّم · ابحث · أنشئ · برمج · حلّل · استكشف + مكتبتي.
- **ست مساحات نطاقية منجزة** (تعلّم/ابحث/أنشئ/برمج/حلّل/استكشف) و**بوابة مشتركة واحدة** (اسأل على /app/chat — النموذجية الوحيدة المتبقية).
- **الترتيب المنتجي الحاكم:** النية → مساحة العمل → السياق → التنفيذ → مخرج منظم → الحفظ → الاستئناف.
- **القاعدة الذهبية للجودة:** المنطق قبل الزينة — كل زر له أثر ملموس، أو يُحذف.

## 1. الوصول والبيانات السرية 🔑

| البند | القيمة |
|---|---|
| المستودع | `github.com/zoih6/minsaj-ai-platform` — **عام** |
| الفرع الإنتاجي | `main` فقط |
| النشر | Vercel — مشروع `minsaj-ai-platform` · فريق `4zobir89-lab` · نشر تلقائي من main |
| الإنتاج | `minsaj-ai-platform-4zobir89-labs-projects.vercel.app` |
| متغيرات البيئة | **لا يوجد أي متطلب حاليًا** — التطبيق مغلق على mock |

> 🔴 **قاعدة الأسرار:** المستودع عام — **يُمنع منعًا باتًا** كتابة أي توكن أو مفتاح أو كلمة مرور في أي ملف يُدفع. بيانات الاعتماد (GitHub PAT / Vercel Token) يقدمها المالك **داخل الجلسة** فقط، وتُستخدم من ذاكرة الجلسة وتُنسى — ولا تُخزن في أي ملف.

## 2. البنية التقنية

| البند | القيمة |
|---|---|
| الإطار | Next.js 16 (App Router) + React 19 + TypeScript 5 |
| الأنماط | `src/app/[locale]/...` — كل شيء تحت مسار اللغة (ar/en) |
| الحزم | مساحة عمل npm: `packages/contracts` (Zod) · `i18n` · `mock-api` · `ui` |
| الهوية البصرية | CSS معماري: `src/app/universal.css` يستورد 14 طبقة من `src/app/styles/universal/` |
| الأيقونات | lucide-react 1.44.0 (المصدر الوحيد) |
| Node | 24.x (يطابق Vercel) |

**الأوامر:**

```bash
npm ci                  # تثبيت حتمي
npm run dev             # localhost:3000
npm run build           # يجب أن ينجح بكل المسارات (~29)
npm run lint            # نظيف قبل الدفع
npm run lint:css        # G-7 stylelint — ratchet ‏929 (الزيادة = فشل)
npm run lint:tokens -- --enforce --compare docs/03-design/reconstruction/evidence/baselines/phase5/g7-baseline.json   # G-7 — وضع الخطأ
npm run qa:shell        # مسبار G-6 (يتطلب خادم بناء يعمل)
npx tsc --noEmit        # أخطاء foundation-harness المعروفة وحدها مقبولة
```

## 3. خريطة الملفات الأساسية

```text
src/
├── app/
│   ├── [locale]/
│   │   ├── (marketing)/page.tsx        # صفحة الهبوط
│   │   ├── app/
│   │   │   ├── layout.tsx              # الغلاف (App Shell) لكل /app/*
│   │   │   ├── home/                   # «لك» الرئيسية
│   │   │   ├── chat/                   # اسأل — البوابة المشتركة (الاصطلاح: الهوية ask · المسار /chat)
│   │   │   ├── [service]/page.tsx      # 6 خدمات + library (بوابة السجل)
│   │   │   ├── learn|research|create/  # مساحات نطاقية (domain)
│   │   │   ├── agents|flows/…          # أدوات متقدمة (+new/+edit)
│   │   │   ├── models/                 # كتالوج النماذج (+[modelId] ديناميكي + routing)
│   │   │   ├── runs/                   # في مجموعة «التشغيل» (KI-1 مغلق)
│   │   │   └── usage|billing|team/     # في مجموعة «التشغيل» · skills/tools عبر تبويبات الكتالوج
│   │   └── preview/                    # أسطح تحقق معزولة (foundation + شرائح code/analyze/explore)
│   ├── universal.css                   # مدخل CSS المعماري
│   └── styles/universal/*.css          # 14 طبقة (foundations/shell/workspaces…)
├── components/
│   ├── app-shell/app-shell.tsx         # الغلاف + مصفوفات التنقل الأربع
│   ├── universal/service-workspace.tsx # بوابة الخدمة (الأقسام التسعة)
│   ├── domain/                         # مساحات نطاقية + Ops + prototypes
│   └── marketing/site-hero.tsx         # بطل الهبوط
├── features/service-workbench/service-registry.ts   # سجل الخدمة الحاكم
└── lib/universal-content.ts            # بيانات الخدمات (eyebrow/starters/outputTitle)
```

**جدول URL → ملف:** انظر [docs/02-experience/information-architecture.md §3](./docs/02-experience/information-architecture.md) — الجدول الكامل الـ34 مسارًا.

## 4. القرارات المعمارية غير القابلة للكسر

1. **سجل الخدمة** هو نقطة قلب أي مساحة من نموذجية إلى نطاقية — لا تُبنى مساحة خارج السجل.
2. **اتجاه الاعتماد صاعد:** مسارات → سجل → مكونات → حزم. الحزم لا تستورد من src.
3. **CSS بالطبقات فقط:** لا CSS مضمن في المكونات؛ القيم الجديدة تدخل `styles/universal/*`.
4. **i18n إلزامي:** كل نص عبر قواميس `packages/i18n` — لا نص حرفي في JSX (الاستثناءات الموثقةة في domain prototypes تُرحل تدريجيًا).
5. **mock-api مصدر البيانات الوحيد** — لا عملاء شبكة قبل قرار المالك.
6. **قانون الأسرار §1.**

## 5. الطبقات الوظيفية الحالية

| الطبقة | المكونات | الحالة |
|---|---|---|
| الغلاف | app-shell + لوحة أوامر ⌘K (19 وجهة) + تنبيهات | مستقرة |
| البوابة المشتركة | service-workspace (9 أقسام) لاسأل على /app/chat فقط | **منطق الاختيار مربوط بالكامل (KI-3 مغلق): المصفوفة الرباعية + موجّه/سريع + إتاحات حية + حفظ** |
| المساحات النطاقية | learn/research/create + code/analyze/explore (W-3: قلب عبر السجل مع sliceReceipt وسطح تحقق لكل شريحة) | منجزة |
| الأدوات المتقدمة | projects/agents/flows/knowledge/models | منجزة (mock) |
| طبقة التشغيل | runs/usage/billing/team/skills/tools | منجزة (mock) — **في التنقل (KI-1 مغلق)** |
| المكتبة | library + شريط «من مكتبتك» | منجزة (حفظ محلي) |

## 6. ما يعمل (مفحوص حاليًا)

- البناء الإنتاجي ينجح بكل المسارات (~33 route / 66 صفحة مضاعفة اللغة) والنشر الإنتاجي `READY`.
- **W-3.5/P-1 منجز (2026-09-21):** بطل الهبوط يُعرض فورًا بلا اعتماد على JS (كان رهين window load = صفحة بيضاء على الاتصالات البطيئة) + كشف المحتوى أبكر (+15% هامشًا قبل دخول الشاشة، 14px/320ms) — انتهى عصر «المحتوى يطارد التمرير». تشخيص «الواجهات خاربة» جرى هندسيًا: 9/12 ادعاءً من VLM هلوسات موثقة؛ البند الوحيد المفتوح: P-5 الهوية البصرية للهبوط (قرار المالك).
- 37 مسارًا مفحوصًا ترجع 200 محليًا (عربي/إنجليزي، الخدمات السبع، المكتبة، الإدارة، أسطح التحقق الأربعة).
- البوابة المشتركة (ask/chat): آلة حالات كاملة (idle→composing→error→clarifying→working→ready→saved) + بدايات سريعة تعبّئ الملقّن + المصفوفة الرباعية مربوطة بالأداة القائدة.
- **W-3 منجزة (2026-09-21):** برمج (مراجعة تغيير مقترح: خطة/فرق/فحوص/إيصال أثر) + حلّل (حساب محلي حتمي: منبع/خطة/نتيجة/تحقق إفردي) + استكشف (خريطة-قائمة/عقدة/رحلة بتقليم متقاعس/توقف) — كلها عبر السجل بأسرها الستة + أسطح تحقق /preview/service-* + إيصالات sliceReceipt.
- ثيمان + اتجاهان بلا تمرير أفقي عند 390/768/1440/1920.
- لوحة الأوامر والتنقّل رباعي المجموعات (19 وجهة) — طبقة التشغيل داخل التنقل (KI-1 مغلق).

## 7. المشاكل المعروفة — اقرأ قبل أي عمل

القائمة الكاملة بدرجات الخطورة وبروتوكول العيب الجديد في [docs/05-process/known-issues.md](./docs/05-process/known-issues.md). الرؤوس:

- **KI-1** 🔴 → **مغلق (2026-09-21):** طبقة التشغيل دخلت التنقل (مجموعة رابعة «التشغيل»).
- **KI-2** 🟠 → **مغلق كتشخيص خاطئ (2026-09-21):** مجلد `[modelId]` سليم بايت-ببايت؛ الوهم سببه ابتلاع مخرجات الطرفية للتسلسل `[m` — القاعدة: تحقق بايت-محمي قبل إعلان اسم معطوب.
- **KI-3** 🔴 → **مغلق (2026-09-21 — W-1 منجز):** أدوات العمل وأسلوب التنفيذ مربوطة بالكامل (المصفوفة الرباعية + موجّه/سريع + إتاحات حية + حفظ حقيقي).
- **KI-4** 🟡 → **مغلق (2026-09-21):** اصطلاح معتمد وموثّق — «الهوية `ask` · المسار `/chat`» (IA §3/§6).
- **KI-5** 🟡 الحفظ محلي بالجلسة (مقصود في مرحلة الأساس).
- **KI-6** 🟢 أخطاء tsc معروفة معفاة في أسطح المعاينة.

## 8. سير العمل النموذجي لمهمة جديدة

1. **اقرأ:** هذا الملف + ONBOARDING + known-issues + وثيقة نطاق مهمتك من `docs/` (الفهرس: `docs/00-INDEX.md`).
2. **اقرأ سجل العمل:** `worklog.md` — ملاحظات كل جلسة سابقة.
3. **شغّل محليًا:** `npm run dev` → localhost:3000 → تفقد صفحات نطاق مهمتك.
4. **خطط:** حدد المتطلبات المتأثرة من `requirements-app.md` والوثيقة الحاكمة للنطاق.
5. **نفّذ:** تفضيل تعديل الملفات القائمة؛ الملف الجديد يحتاج موضعًا طبقيًا صحيحًا (§3) وذكرًا في الوثائق.
6. **بوابات الجودة (بالترتيب):** `npm run build` نجاح → `npm run lint` نظيف → `tsc` بلا أخطاء جديدة → فحص بصري بالمتصفح (ثيمان × اتجاهان + 390px) → تحديث الوثائق المتأثرة **في الكوميت نفسه**.
7. **احفظ:** كوميت ب convention (`feat|fix|docs|refactor|polish(scope): وصف`) يشير إلى معرّفات FR/KI عند وجودها.
8. **ادفع:** `git push origin main` (❌ ممنوع --force).
9. **تحقق من الإنتاج:** دقيقتان → افتح URL الإنتاج → فحص مسارات التأثر → HTTP 200.
10. **وثّق:** سطر CHANGELOG + قسم في worklog.md (Task ID · Agent · Task · Work Log · Stage Summary).

## 9. بروتوكول فشل النشر

| العرَض | الإجراء |
|---|---|
| فشل بناء على Vercel | اقرأ سجل البناء → أصلح محليًا مع بناء ناجح → دفعة إصلاح — لا revert إلا إذا استعصى خلال محاولتين |
| الموقع يرجع خطأ بعد نشر ناجح | أعد النشر من كوميت سليم سابق (revert) فورًا ثم شخّص بهدوء |
| كسر بصري إنتاجي | أرجِعه بـ revert — «الإنتاج لا يبقى معطوبًا أثناء التشخيص» |
| حذف ملفات | 🔴 ممنوع بدون تأكيد المالك أو توثيق كامل في CHANGELOG |

## 10. جهات الاتصال والتقسيم

- **مالك المنتج:** المستخدم (عربي أولًا — كل الحوار بالعربية).
- **التقسيم المرجعي للجلسة القادمة:** **بانتظار اعتماد بوابة خروج Phase 6 من المالك — وهي بوابة خروج W-DS كلها؛ باعتمادها يُتخذ قرار فتح W-4 (الحفظ والاستئناف الحقيقيان FR-3.4/3.5/4.3).** الحراس صارت حية بوضع الخطأ في CI (G-7 ratchet · مسبار G-6 · مصفوفة الاعتماد §5 مع سجل SIGN-OFF-LOG) — أي دفعة قادمة تحترمها أو تحمر. D-3 (UNC-01) ما يزال مفتوحًا على المالك. ملاحظات مرفوعة للمالك من Phase 5 (لا تزال قائمة): ملف `chat-prototype.tsx` ميت (صفر مستوردين) ويستحق قرار حذف · بقايا لهجات `--sp-*` الداخلية وVIS-02 (الأدوار الدلالية للألوان) خارج نطاق المراحل المنفذة. التفصيل في [docs/05-process/roadmap.md](./docs/05-process/roadmap.md).

---

> **الوصية الختامية:** هذا المشروع يُدار بالوثائق لا بالذاكرة. إن وجدت نفسك تعمل بلا مرجع وثائقي لمهمتك — توقف واكتب الوثيقة أولًا. استمرارية الجودة بين الجلسات تعني: كل جلسة تبدأ من هنا وتنتهي بتحديث هنا.
