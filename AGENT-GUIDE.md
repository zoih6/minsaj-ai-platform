# AGENT-GUIDE — القواعد الحمراء
## تُقرأ قبل أي كوميت أو دفعة · مخالفتها = كارثة

> **هذا الملف يحمي المشروع من الكوارث.** القواعد مستخلصة من دروس فشل حقيقية في مشاريع مماثلة. كل قاعدة تُقرأ سببها — فالفهم يمنع الانتهاك.

---

## 🔴 القواعد العشر الحرجة

### 1️⃣ لا تضع أسرارًا في مستودع عام

```
⛔ محظور: كتابة أي PAT / توكن / كلمة مرور في أي ملف يُدفع (حتى مؤقتًا، حتى في worklog)
⛔ محظور: تجاهل تحذير أدوات فحص الأسرار

✅ التوكنات تأتي من المالك داخل الجلسة، تُستخدم من الذاكرة، وتُنسى.
✅ عند انتهاء الجلسة يُنصح المالك بتجديد أي توكن شاركه في محادثة.
```

**السبب:** المستودع عام على الإنترنت — أي سر مدفوع يُستخرج آليًا خلال دقائق ويعرّض الحساب والنشر بالكامل.

### 2️⃣ لا تدفع بلا بوابات الجودة

```
⛔ محظور: git push قبل نجاح: build + lint + فحص بصري للنطاق المتغير

✅ الترتيب الملزم: build → lint → tsc (بلا أخطاء جديدة) → فحص متصفح (ثيمان×اتجاهان+390px) → push
```

**السبب:** النشر تلقائي من main — كل دفعة تظهر للمستخدم خلال دقيقتين. لا يوجد «بيئة تجريبية» تحميك.

### 3️⃣ لا تستخدم git push --force أبدًا

```
⛔ محظور: --force على main
✅ عند اللزوم القصوي: إذن المالك الصريح + --force-with-lease
```

**السبب:** فقدان تاريخ الكوميتات + كسر مزامنة Vercel + استحالة تشخيص ماذا تغير.

### 4️⃣ لا تعدل CSS النظامي داخل مكون أو صفحة

```
⛔ محظور: <style> أو قيم مضمّنة تعالج «مشكلة بصرية» محليًا
✅ الحل يدخل طبقة styles/universal/* المناسبة + تحديث design-system.md في الكوميت نفسه
```

**السبب:** القيمة المحلية تنتشر نسخًا حتى تصبح 15/16/17px «قيمًا رسمية» — هذا هو درب التشتت البصري الأول.

### 5️⃣ لا تبنِ زرًا بلا أثر منطقي

```
⛔ محظور: زر/بطاقة/تبديل «يضيء فقط» دون تغيير مسار أو بنية أو بيانات
✅ كل تفاعل يسجل في interaction-logic.md §5 (جدول التفاعلات) بأثره الملموس
```

**السبب:** «الزينة بلا منطق» هي الشكوى المؤسِّسة لهذه المرحلة من المشروع — أي تكرار لها تراجع عن عقد المنتج.

### 6️⃣ لا تحذف ملفات أو صفحات دون توثيق وسبب

```
⛔ محظور: rm/git rm بدون: تأكيد المالك (للكبير) أو توثيق كامل في CHANGELOG (للصغير)
✅ قبل الحذف: Grep للتأكد من صفر استيرادات + تحديث جداول IA
```

**السبب:** حذف ملف مستورد يكسر البناء — وحذف صفحة موصولة يصنع مسارًا ميتًا أو صفحة يتيمة جديدة.

### 7️⃣ لا تضف مسارًا خارج خريطة IA

```
⛔ محظور: إنشاء page.tsx جديد دون صف له في جدول المسارات
✅ الترتيب: صف في information-architecture.md §3 → مدخل تنقل أو رابط سياقي → ثم الكود
```

**السبب:** الصفحة بلا مدخل = صفحة يتيمة — وهذا بالضبط العيب البنيوي KI-1 الذي نعالجه الآن.

### 8️⃣ لا تكسر آلة الحالات القائمة

```
⛔ محظور: إضافة حالة وسيطة أو تجاوز انتقال مشروع في idle→composing→working→ready→saved
✅ أي تغيير على الآلة: تحديث مخططها في interaction-logic.md §4 أولًا
```

**السبب:** آلات الحالات تتحول بالتدريج إلى «شجرة حالات» لا يفهمها أحد — وإصلاحها لاحقًا أغلى من بنائها.

### 9️⃣ لا تتجاهل الوصولية

```
⛔ محظور: نشر تفاعل بلا aria صحيحة / هدف < 44px / تباين ناقص
✅ كل تفاعل مبدّل يحمل aria-pressed — كل حوار Radix بسماته — كل زر أيقوني بـ aria-label
```

**السبب:** الوصولية في هذا المنتج P0 وليست تحسينًا لاحقًا (NFR-8) — ومخالفتها تراجع حرفي للمتطلبات.

### 🔟 لا تنهِ جلسة بلا توثيق

```
⛔ محظور: إنهاء العمل دون: سطر CHANGELOG + قسم worklog.md + تحديث AGENT.md إن تغيرت الحقائق
✅ الجلسة التالية تعتمد كليًا على ما دونته الآن — الوثيقة الناقصة = عمل سيتكرر أو يُكسر
```

**السبب:** استمرارية المشروع بين الجلسات قائمة على سلاسل الوثائق — لا على ذاكرة أي شخص.

---

## 🟡 قواعد صفراء (تحتاج انتباهًا لا حظرًا)

| # | القاعدة | التفصيل |
|---|---|---|
| Y-1 | تعديل `universal.css` يستلزم حذرًا مضاعفًا | طبقة v21 المضمّنة حل تعطل Turbopack — لا تُفك عشوائيًا |
| Y-2 | تعديل عقود Zod = تحديث mock-api + كل المستهلكين في كوميت واحد | data-model §6 |
| Y-3 | ترقية Next/React ليست «صيانة روتينية» | قرار يوثق سببه ومخاطره أولًا |
| Y-4 | النصوص في domain prototypes مضمّنة (Inline ar/en) | وضع قائم يُرحّل تدريجيًا عبر i18n — لا تضف نصًا مضمّنًا جديدًا |

## بروتوكول ما قبل الدفعة (Checklist)

```text
[ ] build ناجح                     [ ] lint نظيف
[ ] tsc بلا أخطاء جديدة            [ ] فحص بصري: ثيم فاتح + داكن
[ ] فحص اتجاهين ar/en              [ ] 390px بلا تمرير أفقي
[ ] الوثائق المتأثرة حُدثت          [ ] لا أسرار في diff
[ ] كوميت يتبع convention          [ ] CHANGELOG + worklog جاهزان
```

**القسمة النهائية للحكم:** إن كنت مترددًا في وضع علامة أمام أي بند — فالإجابة هي «لا تدفع بعد».
