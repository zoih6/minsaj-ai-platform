/**
 * Code slice simulation data — U2.4.
 *
 * Sample projects, deterministic plan/proposal/check/impact rules, and
 * edge-state presets. No React, no fetch, no randomness, no clock: the same
 * inputs always produce the same plan, diff, checks, and impact.
 */

export * from "./projects";
export * from "./plan";
export * from "./presets";
