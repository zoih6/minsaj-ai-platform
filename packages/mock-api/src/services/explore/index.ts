/**
 * Explore slice simulation data — U2.6.
 *
 * Seeded knowledge-graph fixtures and deterministic map/trail rules. No React,
 * no fetch, no randomness, no clock: the same seed always produces the same
 * map, and the same trail always resumes to the same stage.
 */

export * from "./graph";
