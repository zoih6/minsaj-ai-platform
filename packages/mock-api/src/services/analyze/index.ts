/**
 * Analyze slice simulation data — U2.5.
 *
 * Dataset fixtures, deterministic profile/plan/compute/verification rules, and
 * edge-state presets. No React, no fetch, no randomness, no clock: the same
 * inputs always produce the same profile, plan, result, and verification.
 */

export * from "./datasets";
export * from "./presets";
