# Minsaj AI Platform — PRD

## 1. ملخص المنتج

**Minsaj AI** منصة عمل موحّدة للذكاء الاصطناعي، عربية أولًا، تساعد المستخدم على الانتقال من النية أو السؤال إلى نتيجة منظمة يمكن فهمها وتعديلها وحفظها والعودة إليها لاحقًا.

بدل أن يبدأ المستخدم باختيار نموذج أو أداة، يبدأ من المهمة التي يريد إنجازها. ثم يوجّه المنتج المستخدم إلى مساحة العمل المناسبة ضمن تجربة واحدة متماسكة:

```text
النية → مساحة العمل → السياق → التنفيذ → مخرج منظم → الحفظ → الاستئناف والتطوير
```

## 2. المشكلة التي يعالجها المنتج

يعتمد المستخدمون حاليًا على أدوات ومحادثات منفصلة لإنجاز مهمة واحدة. ينتج عن ذلك فقدان السياق، تكرار شرح المطلوب، صعوبة اختيار الأداة المناسبة، نتائج غير منظمة، وصعوبة العودة إلى العمل السابق. كما تعاني المنتجات العامة من ضعف دعم العربية واتجاه RTL، ومن واجهات توحي أحيانًا بأن بحثًا أو تنفيذًا قد حدث بينما لم يحدث فعليًا.

## 3. الحل

يوحّد Minsaj AI الخدمات الأساسية في مساحة عمل واحدة، ويقسمها إلى مسارات واضحة:

| الخدمة | الغرض الحالي |
|---|---|
| **Ask / اسأل** | محادثة مباشرة مع المساعد. |
| **Learn / تعلّم** | مسارات تعلم ومحتوى تعليمي منظم. |
| **Research / ابحث** | البحث والتحليل وتجميع النتائج. |
| **Create / أنشئ** | إنشاء المستندات والعروض والمحتوى المرئي. |
| **Code / برمج** | مساحة مخصصة لمهام البرمجة. |
| **Analyze / حلّل** | تحليل المعلومات والبيانات. |
| **Explore / استكشف** | استكشاف الخدمات والوجهات والمحتوى. |
| **My Library / مكتبتي** | حفظ النتائج والعودة إلى الأعمال السابقة. |

وتدعم المنصة أدوات تشغيل متقدمة لإدارة المشاريع والوكلاء والتدفقات ومصادر المعرفة والنماذج.

## 4. وعد المنتج

> من فكرة بسيطة إلى نتيجة يمكنك فهمها وتعديلها وحفظها والعودة إليها.

## 5. المستخدمون المستهدفون

| المستخدم | الاحتياج الأساسي |
|---|---|
| المحترف المستقل | البحث والتلخيص وإعداد مخرجات جاهزة للتسليم. |
| صانع المحتوى | تحويل الفكرة إلى موجز ومحتوى منظم قابل للتطوير. |
| المطور | فهم الكود وتخطيط التغييرات ومراجعتها بأمان. |
| المتعلم | الحصول على مسار تعليمي واضح يمكن استئنافه. |
| الفريق الصغير | تنظيم المشاريع والأدوار وسجل العمل في مراحل لاحقة. |

## 6. نطاق النسخة الحالية

النسخة الحالية هي **أساس واجهة وتجربة المنتج**، وتحتوي على:

- واجهة عربية أولًا مع دعم العربية والإنجليزية واتجاهي RTL وLTR.
- غلاف تطبيق موحد وقائمة جانبية للتنقل بين الخدمات.
- صفحات التسويق، التطبيق، المعاينة، الإدارة، ومساحات العمل.
- مساحات Learn وResearch وCreate ومكوّنات Service Workbench.
- واجهات المشاريع والوكلاء والتدفقات والتشغيلات ومصادر المعرفة والنماذج.
- طبقة بيانات typed mock عبر `packages/mock-api` بدل الخدمات الخلفية الحقيقية.
- أصول العلامة التجارية داخل `public/brand/`.
- بنية responsive تشمل drawer للهاتف وrail للتابلت وقائمة موسعة لسطح المكتب.

## 7. قائمة التنقل الحالية

يتم تعريف القائمة الجانبية في:

```text
src/components/app-shell/app-shell.tsx
```

وتضم:

```text
/{locale}/app/home       # لك — For you
/{locale}/app/chat       # اسأل — Ask
/{locale}/app/learn      # تعلّم — Learn
/{locale}/app/research   # ابحث — Research
/{locale}/app/create     # أنشئ — Create
/{locale}/app/code       # برمج — Code
/{locale}/app/analyze    # حلّل — Analyze
/{locale}/app/explore    # استكشف — Explore
/{locale}/app/library    # مكتبتي — My library
```

وتحت الأدوات المتقدمة:

```text
/{locale}/app/projects   # المشاريع
/{locale}/app/agents     # الوكلاء
/{locale}/app/flows      # التدفقات
/{locale}/app/knowledge  # مصادر المعرفة
/{locale}/app/models     # النماذج
```

وتوجد صفحات مساحة المستخدم والإدارة في:

```text
/{locale}/app/settings
/{locale}/app/billing
/{locale}/app/usage
/{locale}/app/team
```

## 8. خريطة الصفحات

تستخدم الصفحات متغير اللغة `[locale]`، مثل `ar` أو `en`:

```text
src/app/[locale]/
├── (marketing)/page.tsx
├── layout.tsx
├── loading.tsx
├── not-found.tsx
│
├── app/
│   ├── page.tsx
│   ├── layout.tsx
│   ├── loading.tsx
│   ├── error.tsx
│   ├── home/page.tsx
│   ├── chat/page.tsx
│   ├── [service]/page.tsx
│   ├── agents/
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [agentId]/edit/page.tsx
│   ├── flows/
│   │   ├── page.tsx
│   │   ├── new/page.tsx
│   │   └── [flowId]/edit/page.tsx
│   ├── projects/
│   │   ├── page.tsx
│   │   └── [projectId]/page.tsx
│   ├── runs/
│   │   ├── page.tsx
│   │   └── [runId]/page.tsx
│   ├── knowledge/
│   │   ├── page.tsx
│   │   └── [collectionId]/page.tsx
│   ├── models/
│   │   ├── page.tsx
│   │   ├── routing/page.tsx
│   │   └── [modelId]/page.tsx
│   ├── billing/page.tsx
│   ├── usage/page.tsx
│   ├── team/page.tsx
│   ├── settings/page.tsx
│   ├── skills/page.tsx
│   └── tools/page.tsx
│
└── preview/
    ├── page.tsx
    └── service-foundation/page.tsx
```

## 9. الخدمات الديناميكية

الخدمات `learn` و`research` و`create` و`code` و`analyze` و`explore` و`library` تستخدم المسار العام:

```text
src/app/[locale]/app/[service]/page.tsx
```

وتتولى ملفات الميزات بناء الواجهات الفعلية:

```text
src/features/learn/
src/features/research/
src/features/create/
src/features/service-workbench/
```

## 10. البنية التقنية الحالية

```text
src/                    # تطبيق الموقع
packages/               # العقود والترجمة وبيانات المحاكاة وواجهات UI
public/                 # الأصول العامة والعلامة التجارية
package.json            # تعريف المشروع والاعتماديات وأوامر التشغيل
package-lock.json       # قفل إصدارات الاعتماديات
next.config.ts          # إعداد Next.js
tsconfig.json          # إعداد TypeScript
postcss.config.mjs      # إعداد PostCSS
eslint.config.mjs       # إعداد ESLint
components.json         # إعداد مكوّنات الواجهة
```

توجد المكوّنات الرئيسية في:

```text
src/components/app-shell/
src/components/domain/
src/components/marketing/
src/components/theme/
src/components/universal/
```

وتوجد الحزم الداخلية في:

```text
packages/contracts/
packages/i18n/
packages/mock-api/
packages/ui/
```

## 11. مبدأ الحقيقة والتنفيذ

لا ينبغي للواجهة أن توحي بأن بحثًا أو استدعاء أداة أو تنفيذًا خارجيًا حدث إذا لم يحدث فعليًا. النسخة الحالية تعتمد على بيانات محاكاة typed mock لتوضيح التجربة، مع إبقاء الحدود بين الواجهة والتنفيذ الحقيقي واضحة تمهيدًا لاستبدال طبقة المحاكاة بخدمات فعلية.

## 12. مراحل المنتج

| المرحلة | الهدف |
|---|---|
| **R0 — UI Foundation** | الأساس البصري والواجهات والمسارات الحالية ببيانات محاكاة. |
| **R1 — Interactive Prototype** | حالات التقدم والإلغاء وإعادة المحاولة والمخرجات والإصدارات. |
| **R2 — Personal Alpha** | الحسابات والحفظ الدائم والمشاريع الشخصية والمكتبة الدائمة. |
| **R3 — Real AI Services** | مزوّدو النماذج والبحث المباشر والاستشهادات. |
| **R4 — Controlled Automation** | وكلاء ومهارات وتدفقات محددة مع موافقات صريحة. |

## 13. الحالة التشغيلية الحالية

يمثل المنتج حاليًا أساس تجربة وواجهة متكاملًا لمنصة Minsaj AI، مع مسارات واضحة للخدمات الرئيسية، وقائمة تنقل موحدة، ومساحات عمل متخصصة، وواجهات إدارة للموارد المتقدمة.

تستخدم التجربة الحالية طبقة بيانات محاكاة typed mock لعرض الرحلات والواجهات وسلوكيات المنتج بصورة متماسكة. وتشكل هذه الطبقة حدًا واضحًا يمكن استبداله بخدمات الذكاء الاصطناعي والتخزين والبحث الفعلية عند الانتقال إلى مراحل التشغيل الإنتاجي.

تشمل القيود الحالية اعتماد بعض الرحلات على بيانات محاكاة، وعدم اكتمال التكاملات الإنتاجية مثل الحسابات، التخزين الدائم، مزوّدي النماذج، البحث المباشر، وقاعدة البيانات.

## 14. تعريف النجاح

- يفهم المستخدم نقطة البداية دون تدريب مسبق.
- يصل المستخدم إلى أول مخرج مفيد بسرعة متزايدة.
- ترتفع نسبة حفظ المخرجات واستئنافها.
- لا توحي الواجهة بتنفيذ إجراء لم يحدث.
- تبدو العربية وRTL جزءًا أصيلًا من المنتج، لا ترجمة لاحقة.

## 15. نقاط البدء للمطور

```text
src/app/[locale]/
src/components/app-shell/app-shell.tsx
src/features/
packages/
public/
README.md
```
