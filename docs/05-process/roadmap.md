# خارطة الطريق — Roadmap
## أولويات المراحل القادمة بترتيب منطقي

> **الغرض:** ترتيب العمل القادم بحيث يُبنى كل شيء على أساس مستقر. القاعدة: **منطق قبل جمال، هيكلة قبل ميزات.**

---

## المرحلة W-1 · ربط منطق الاختيار (الأولوية الأولى) — **منجزة (2026-09-21)**

> «حين يختار المستخدم نمط العمل وأسلوب التنفيذ، تُهيكل الصفحة بدقة — لا زينة.»

| المهمة | المرجع | قبول | الحالة |
|---|---|---|---|
| تنفيذ مصفوفة أداة العمل (التلميح/المخرج/البدايات/الإتاحات) | interaction-logic §2 | I-1 كامل الأثر | **منجز** |
| تنفيذ سلوك موجّه/سريع (أسئلة توضيحية مقابل تنفيذ مباشر) | interaction-logic §3 | I-2 كامل الأثر | **منجز** |
| ربط حبتي الإتاحة (ملف/صوت) بسلوك حقيقي (منتقي/حالة) | interaction-logic §5 I-9 | إتاحة قابلة للملاحظة | **منجز** |
| نقل تعريف أدوات الخدمات إلى `universal-content.ts` (بيانات لا كود) | architecture §3 A-5 | المكوّن يستهلك البيانات | **منجز** |

**بوابة الخروج: عبرت — FR-3.1–3.3 منجزة + مراجعة بصرية (ثيمان × اتجاهان + 390px) نظيفة + بناء نظيف.**

## المرحلة W-2 · إصلاح البنية (IA) — **منجزة بنيويًا (2026-09-21)**

| المهمة | المرجع | قبول | الحالة |
|---|---|---|---|
| قرار استضافة طبقة التشغيل في التنقل (مجموعة رابعة) | known-issues KI-1 | FR-1.7 منجز | **منجز** |
| إصلاح مجلد `[modelId]` | known-issues KI-2 | مسار النموذج يعمل | **أُغلق كتشخيص خاطئ** |
| تحديث جداول IA بعد القرار | information-architecture | جدول مطابق للكود حرفيًا | **منجز** |

## المرحلة W-3 · إكمال المساحات النطاقية — **منجزة (2026-09-21)**

| المهمة | المرجع | الحالة |
|---|---|---|
| توثيق بنى تعلّم/ابحث/أنشئ الحالية في page-specs | القاعدة: لا تعديل قبل توثيق | **منجز** (D-0/D-1/D-2/D-3) |
| شريحة برمج: عقود+قاموس+mock+ميزة+CSS+قلب السجل | U2.4 · بنمط learn/research/create | **منجز** (D-4 + /preview/service-code + sliceReceipt) |
| شريحة حلّل | U2.5 | **منجز** (D-5 + /preview/service-analyze + sliceReceipt) |
| شريحة استكشف | U2.6 | **منجز** (D-6 + /preview/service-explore + sliceReceipt) |

**بوابة الخروج: عبرت** — الخدمات السبع كلها مساحات نطاقية عبر السجل + بناء نظيف (66 صفحة) + فحص بصري محيط (ثيمان × اتجاهين + 390px) + توثيق كامل في نفس كوميت كل شريحة.

## المرحلة W-3.5 · صقل الواجهات (UI/UX Polish) — **P-1 منجز (2026-09-21) · البقية بانتظار توجيه المالك**

> تسبق W-4: لا يُبنى حفظ حقيقي فوق واجهة يرفضها المالك. التشخيص جرى بتحقق بايت-آمن عبر DOM/CSS هندسيًا (لا ادعاءات VLM غير معاينة) — **قاعدة الجلسة السابقة طُبّقت حرفيًا: 9 من 12 ادعاءً بصريًا ثبت أنها هلوسة VLM** (مؤشر التنقل، مزج لاتيني، تباين placeholder، الفراغ الميت 65%، بطاقات راديو وهمية، أهداف لمس ضئيلة، فشل تباين الداكن، خلفيات بيضاء في الداكن، عدم اتساق المساحات).

| # | المهمة | الحالة والأدلة |
|---|---|---|
| P-1 | ترويض إيقاع الظهور: إزالة اعتماد عناصر البطل على JS (تُعرض فورًا حتى قبل الترطيب)، وتفعيل أبكر (+15% هامش سفلي بدل −8%)، وإزاحة أصغر (14px بدل 30px)، ومدة أقصر (320ms بدل 480ms) | **منجز** — site-hero.tsx (إزالة reveal عن 5 عناصر فوق الطية) + reveal-fx.tsx (rootMargin 15%/threshold 0) + marketing.css:70 (14px/320ms) |
| P-2 | «الفراغ الميت في لك» | **سقط الادعاء** — القياس الهندسي: قسم الخدمات يبدأ عند y=667 من إطار 900px؛ الصفحة ممتلئة |
| P-3 | «الداكن طيني/تباين فاشل» | **سقط الادعاء** — فحص 89 عنصرًا نصيًا بتركيب ألفا صحيح في لك/البوابة: صفر فشل AA؛ ولا خلفيات بيضاء في الداكن |
| P-4 | «عدم اتساق المساحات» | **سقط الادعاء** — المساحات الخمس كلها على هيكل u2-workbench موحد والرقائق متطابقة حرفيًا (999px/44px/نفس الألوان) |
| P-5 | صقل إيقاع البطل التسويقي: الرسمة العامة + الهوية البصرية | **مفتوح بانتظار المالك** — تحسين جوهري (هوية بصرية/رسمة/لوحة) قرار منتجي لا يُنفذ أحاديًا |

**بوابة الخروج (المحدثة):** بناء نظيف + فحص بصري بعد P-1 + عرض لقطات ما-بعد على المالك للتوجيه: إما اعتماد الهوية الحالية والمضي إلى W-4، أو فتح عمل P-5 بإرشاد بصري محدد من المالك.

## المرحلة W-DS · إعادة بناء نظام التصميم — **معتمدة من المالك (2026-09-22) · Phase 0-6 منفذة ومقاسة · بوابات خروج Phase 0-5 مُعتمدة (Phase 3 بشرط §7.5 المُوفى 98/98 · Phase 4 بقرار §7.6 · Phase 5 بقرار §7.7 في 2026-09-24) · بوابة خروج Phase 6 — وهي بوابة خروج W-DS كلها — مُعتمدة (§7.8، 2026-09-25): W-DS مقفلة بالكامل**

> قرار المالك الموثق (2026-09-22): التدقيق الهندسي السابق (W-3.5) صحيح لكنه لا يغطي طبقة **جودة المنتج البصرية** — «التطبيق يعمل لكنه لا يبدو مصممًا كمنصة واحدة». فُتحت مرحلة مستقلة W-DS قبل W-4. **الاعتماد وُثّق (2026-09-22) في §7.1 من الوثيقة الرئيسية: المواصفة مُلزِمة للتنفيذ اللاحق، ولا تُعد أي مشكلة محلولة قبل اجتياز Gate A + Gate B وتسجيل قياسات جديدة، وW-4 تبقى محجوبة حتى شروط خروج W-DS. اعتماد بدء Phase 1 وُثّق (§7.2) مع قيوده المُلزِمة. بوابة خروج Phase 1 مُعتمدة (§7.3). بوابة خروج Phase 2 مُعتمدة (2026-09-22، §7.4) وPhase 3 (الكروم) نُفّذت وقُيست في اليوم نفسه: رأس صف مفرد 56px شبه مصمت بلا عنوان صفحة (الهوية تعلن مرة واحدة: 107/65px ← 56px على كل مسار) · عامل إرساء عائم واحد لكل المسارات (لهجتا الشريط/الدوك ماتتا: 2 ← 1) · هرمية درج رباعية T1–T4 بتسمية «مساحتي» وتذييل مثبّت وطيّ «التشغيل» على الجوال · الميزانية العمودية 15.5–21.2% ← 14.5% موحدًا @390 و13.1% @430 (البوابة ≤15.5/≤14.5). القياسات في baselines/phase3/. **بوابة خروج Phase 3 مُعتمدة باعتماد مشروط (2026-09-22، §7.5): شرط الإغلاق — توثيق اختبار عدم حجب الـ Mobile Dock لآخر محتوى قابل للتفاعل (خصوصًا الإعدادات وبقية صفحات الهاتف) مع تسجيل نتيجة اختبار التمرير؛ وفور توثيقه تُفتح Phase 4 (الأنماط) وتبقى اختلافات أنماط المحتوى الداخلي داخل نطاقها لا عيبًا في Phase 3.** الشرط وُفّي في الجلسة نفسها (98/98 خلية PASS بصفر حجب وأصغر فجوة 21.9px — `baselines/phase3/dock-occlusion-probe.json`) **وPhase 4 (الأنماط) منفذة ومقاسة فورًا: INT-01 محلول** — البوابة composer-first وفق قالب §2.3 (أقسام 8←3 · بطاقات 18←7 · زر التنفيذ تحت الطية ← فوقها @390 · إفصاح تدريجي واحدًا في كل مرة بمزلاج أول ضغطة · الافتراضي سريع · شريط تقدم رفيع داخل البطاقة · ثقة في التذييل · بدايات كروابط مجردة) **وINT-02 محلول مع تصحيح تشخيص** — مالك واحد للحركة (motion.css) بالقيم المعتمدة 240ms + إزاحة 4px مقيسة حية، وقاعدة shell.css الـ480ms كانت ميتة في التتابع والتدقيق استشهد بالخاسرة. القياسات في `baselines/phase4/` (20 فرقًا كلها على مسار البوابة · صفر فروق في الـ95 خلية الأخرى · شرط الدوك أعيد التحقق 98/98 · الأصناف 67←66). **بوابة خروج Phase 4 مُعتمدة (2026-09-23، §7.6) وPhase 5 (العلامة والشبكة) منفذة ومقاسة في الجلسة نفسها: VIS-01 محلول** — جسر توكنز التسويق وفق DESIGN-TOKENS §9: قيم `@theme` السبع مراجع `var(--u-*)` والهكسات المستقلة الثمانية حُذفت، وتدرج البطل وألوان التحديد والوهجان أعيد تعبيرها على توكنات المنصة، وقاعدة إعادة ربط الزمرد الداكن حُذفت (الجسر يتبدل ذاتيًا مع الثيم)، مع إصلاح داخل الطور لدرجة حبر الزمرد (تباين النص الصغير على الهبوط) · **VIS-03 محلول** — نطاق `--fc-` حُذف بالكامل (32 إعلانًا حيًا ← 0) واللوحة الثانية مُعلنة كتوكن `--u-canvas-focus` تحت `[data-canvas="focus"]` مع ربط `--sp-bg` بالتوكن نفسه (مالك واحد، الرسم مطابق بالبكسل) · **GRD-01 محلول** — إحصاء المقاييس المعلنة عبر CSSOM: 21 قيمة مميزة ← السلم الأربع فقط (narrow 560 / prose 780 / wide 1040 / full 1440) وحذف توكنات --mj-content-max/--u-content-max/--u-reading-max وتوثيق حدود الحوارات ومحفزات CQ. القياسات في `baselines/phase5/` (أحكام المسبار الثلاثة PASS · صفر فروق حقول ×100 خلية · G-7 مطابق للأساس السابق على كل محور). بانتظار اعتماد بوابة خروج Phase 5 → Phase 6 (تقسية QA).

| # | المهمة | المرجع | الحالة
|---|---|---|---|
| DS-0 | تدقيق جنائي UI/UX لكل المسارات الأساسية عند 390/430/768/1024/1440 (قياسات DOM + طبقة إدراكية مربوطة بالقياس) | مصفوفة `reconstruction/UI-UX-AUDIT-MATRIX.csv` (37 نتيجة) | **منجز** |
| DS-1 | وثيقة إعادة البناء المركزية + المبادئ + التوكنز + جرد المكونات | `reconstruction/DESIGN-SYSTEM-RECONSTRUCTION.md` ومرفقاتها | **معتمد (2026-09-22)** |
| DS-2 | معمارية التنقل (ميزانية 390px + هرمية الدرج + نطاقات البحث) | `reconstruction/NAVIGATION-ARCHITECTURE.md` | **معتمد (2026-09-22)** |
| DS-3 | معمارية الاستجابة (تكوين لكل نطاق + حارس المؤشر الخشن + اشتراطات RTL) | `reconstruction/RESPONSIVE-ARCHITECTURE.md` | **معتمد (2026-09-22)** |
| DS-4 | أنماط الصفحات والحركة + بروتوكول QA البصري ذو البوابتين | `reconstruction/PAGE-PATTERNS-AND-MOTION.md` + `VISUAL-QA-CHECKLIST.md` | **معتمد (2026-09-22)** |
| DS-5..10 | التنفيذ على موجات (حراس ← أسس ← سطوح ← كروم ← أنماط ← علامة ثم QA صارم) | §6 من الوثيقة الرئيسية | **Phase 0 منجزة (3508d48) · Phase 1 منفذة ومقاسة (147f752) وبوابة خروجها مُعتمدة (§7.3) · Phase 2 منفذة ومقاسة وبوابة خروجها مُعتمدة (§7.4) · Phase 3 (الكروم) منفذة ومقاسة وبوابة خروجها مُعتمدة باعتماد مشروط (§7.5) وشرطها مُوفى وموثق (98/98) · Phase 4 (الأنماط) منفذة ومقاسة وبوابة خروجها مُعتمدة (§7.6، 2026-09-23) · Phase 5 (العلامة والشبكة) منفذة ومقاسة وبوابة خروجها مُعتمدة (§7.7، 2026-09-24) · Phase 6 (تقسية QA) منفذة ومقاسة في اليوم نفسه: G-7 في وضع الخطأ (ratchet مُختبَر بانحدار محقون) · مسبار G-6 حي في CI (11/11 خلية، قبل/بعد RES-01 في baselines/phase6/) · مصفوفة الاعتماد §5 حية (signoff.mjs + SIGN-OFF-LOG.md مُنفَّذة في CI) · RES-01 محلول (حارس R-RES-1b + محرك عدم التصغير R-RES-1a) — بانتظار اعتماد بوابة خروج Phase 6 = بوابة خروج W-DS** |

**قرارات المالك D-1..D-5 (وُثّقت في §7.1 من الوثيقة الرئيسية):** D-1 شفافية الهيدر — الافتراضي المعتمد (شبه مصمت 0.95) · D-2 تنقل التابلت — الافتراضي المعتمد (شريط جانبي موسّع بمُسميات) · D-3 سياق UNC-01 — ما يزال مفتوحًا بانتظار المالك · D-4 وزن الشريط السفلي — مفتوح كتفضيل؛ القاعدة العاملة: سقف الوزن في معمارية التنقل §5 · D-5 ترتيب التنفيذ — مؤكد كما هو.

**بوابة الخروج:** تنفيذ الموجات (§6) بمخرجات تُقاس بقياسات جديدة عبر بروتوكول البوابتين (Gate A + Gate B) — لا تُعتبر أي مشكلة محلولة قبل ذلك — ثم تفتح W-4 بقرار المالك.

## المرحلة W-6 · نسجام الصفحات (Page Harmony) — **مصرّح بها من المالك ومنفذة ومقاسة (2026-09-25، جلسة §7.8)**

> تفويض المالك الحرفي بعد اعتماد بوابة خروج Phase 6: «اريد الان تغير فعلي في الفرونت اند ونسجام الصفحات» — فوج مرئي يطبّق نظام التصميم المقفل على ما كان موثقًا مفتوحًا في مصفوفة التدقيق. الأداة الجديدة: `scripts/visual-qa/harmony-probe.mjs` + أساس مجمد `evidence/baselines/w6/`.

| # | المهمة | المرجع | الحالة
|---|---|---|---|
| W6-1 | توحيد كل h1 التطبيقية على مستوى السلّم الواحد --mj-text-h1 (حذف كل clamp لكل عائلة) | مصفوفة HIE-01 · R-TYPE-1 | **منجز** — قيم مميزة @390: ‏5←1 (26.24px) و@1440: ‏5←1 (34px) عبر المسارات الـ19 |
| W6-2 | إيقاع أقسام واحد --mj-space-600 لكل العائلات (أعمدة إيقاع + تصفير الهوامش المتراكمة) | مصفوفة SPC-02/03 · R-SPACE-2/3 | **منجز** — الوسائط @390: ‏0/40/14/16/26.1/37.1 ← 26.1 للست · أصغر فجوة ‑116 ← 26.1 |
| W6-3 | معالجة تركيز واحدة (3px ‏--u-primary-glow · إزاحة 3px) + kbd على --mj-text-code | مصفوفة ACC-02 · R-ACC-1 | **منجز** — حذف التوأم غير الطبقي 2px وثمانية حلقات لكل مكوّن |
| W6-4 | تنفيذ قرار D-2: تابلت = شريط جانبي موسّع بمُسميات push (rail خيار المستخدم) | مصفوفة RES-02 · RESPONSIVE §bands | **منجز** — 264px · التسميات ظاهرة · mainPushed=264 · قواعد overlay وُئدت |
| W6-5 | تصحيح سجل المصفوفة: INT-03 محلولة فعليًا في Phase 1 وIMP-02 في Phase 6 | أمانة السجل | **منجز** — لا تُنسب للموجة |

**بوابة الخروج: عبرت** — بناء 66/66 · lint/tsc نظيفان · stylelint 912≤929 · G-7 ‏PASS بتحسن صافي (font-size خام 26←18 · ظلال 59←58 · legacy ‏1097←1095) · G-6 ‏11/11 · التقاط 100 خلية مقابل phase6: ‏187 فرقًا كلها في فئات الموجة الثلاث المقصودة وصفر انحدار (hOverflow صفر ×100 · tapUnder44 مطابق حرفيًا) · فحص إدراكي VLM سليم — الأدلة في `evidence/baselines/w6/`.

**المتبقي الموثق (لا ادعاء):** VIS-02 (الأدوار الدلالية للألوان — أكبر بنود النسجام المتبقية، يحتاج موجته) · TYP-01/04 (إكمال توحيد السلّم وإعادة توازن الأحجام) · SUR-02/03 (الراتشات تحرس الدين المتبقي 12/58) · NAV-06 نصفه الثاني · IMP-01/03 (موجات توحيد اللهجات) · P-5 وD-3 وD-4 وchat-prototype.tsx الميت — كلها بانتظار المالك كما هي.

## المرحلة W-7 · إعادة بناء واجهات الخدمات والتجاوب الحقيقي (Service Surfaces & True Responsiveness) — **مصرّح بها من المالك ومنفذة (2026-09-25، جلسة §7.9)**

> تفويض المالك بعد معاينة الإنتاج منشور W-6: الصفحات الخدمية (ابحث/أنشئ/برمج/حلل/استكشف) «تصميمها ملخبط… تحسه شغل أطفال… الهيكل كويس بس التصميم خارب ui/ux» + الإعدادات غير متجاوبة على الجوال + «كل الصفحات غير متجاوبة في وضع سطح المكتب… في كل المواقع حين اضغط في المتصفح وضع سطح المكتب الموقع تلقائي يتغير حجمه». التشخيص الهندسي للجلسة أكد ثلاث علل جذرية لم تكن في خطة أي مرحلة سابقة (الأدلة: قياسات DOM حية + لقطات 390/1440 + نقد VLM مقارن ضد صفحة اسأل المرجعية):

| # | المهمة | المرجع | الحالة
|---|---|---|---|
| W7-1 | إصلاح «الوادي الميت»: خطأ flex-basis في `.mj-section__head-copy` — `flex: 1 1 300px` مصمم لمحور أفقي، واستعلام الحاوية ≤640px يقلب الأب لعمودي فيصبح 300px **ارتفاعًا** لكل قسم (قياس حي: رأس قسم الإعدادات 300px لمحتوى 48px) — يضرب الإعدادات وكل صفحات ops على الجوال | HIE-02 لاحقًا · قياس DOM الجلسة | **منجز** |
| W7-2 | عكس سياسة RES-01 بقرار مالك (D-6): وضع «سطح المكتب» في متصفح الهاتف يجب أن يقدّم تكوين سطح المكتب (الشريط الجانبي) كما في كل المواقع القياسية — إزالة حارس R-RES-1b (coarse→mobile دائمًا) ومحرك التكبير R-RES-1a (2.37x) من TS وCSS، وربط التكوين بالعرض فقط | قرار المالك D-6 (§7.9) | **منجز** |
| W7-3 | بناء `ServiceProductShell`: الصفحات النطاقية الست (تعلّم/ابحث/أنشئ/برمج/حلل/استكشف) تُقدَّم كمنتج لا كمنصة اختبار — ترويسة خدمية نظيفة (eyebrow/h1/lede بقالب service-welcome) + رقاقة «وضع التجربة» واحدة مهيبة تفتح لوحة تضم كل عدّة المحاكاة (المراحل/التحكم/الحفظ/الإيصال) بدل كروم u2-workbench المكشوف (شارات الجلسة/أزرار التخزين/شريط المراحل/أزرار حفظ-تجريبي) | §7.9 · PAGE-PATTERNS §2.3 | **منجز** |
| W7-4 | توحيد جلد الأسطح النطاقية (u2-research/u2-create/u2-code/u2-analyze/u2-explore/u2-learn) على قواعد قالب service-space: بطاقة واحدة متماسكة لكل مرحلة، أزرار موحدة، إيقاع واحد | §7.9 | **منجز** |
| W7-5 | بوابة الإدراك الحرج (Gate B-11): لا يُقبل «القياسات متسقة» كدليل جودة وحده — مراجعة VLM **نقدية مقارنة** ضد صفحة مرجعية معتمدة (اسأل) بلقطات كاملة عند 390/768/1440 + وضع سطح المكتب على الهاتف، بشرط فشل صريح إذا لم تبلغ الصفحة مستوى المرجع | VISUAL-QA-CHECKLIST (جديد) | **منجز** |

**بوابة الخروج:** بناء نظيف + لقطات بعد لكل مسار متأثر عند 390/1440 + 980-coarse يظهر الشريط الجانبي + نقد VLM مقارن لا يقل عن مستوى المرجع + نشر إنتاجي متحقق.

**تبعيات عولجت معها:** تحديث `shell-probe.mjs` و`harmony-probe.mjs` لسياستها الجديدة (التكوين بالعرض) · توثيق عكس RES-01 في RESPONSIVE-ARCHITECTURE كقرار D-6 · إبقاء حارس 44px للمس على ≤768 الفعلي فقط.

## المرحلة W-4 · الحفظ والاستئناف الحقيقيان — **مفتوحة للجدولة بقرار المالك (حُجبها الأصلي انتهى بإقفال W-DS §7.8؛ المالك وجّه إلى W-6 ثم W-7)**

- ربط الحفظ بطبقة خلفية عبر العقود القائمة (data-model §5).
- FR-4.3: فتح عنصر مكتبة يستأنف تكوينه (خدمة/أداة/أسلوب/ملقّن).
- مسودات تلقائية عند المغادرة (F-1 خطوة 6).

## المرحلة W-5 · الحسابات والمصادقة

خارج نطاق الأساس الحالي — تُفتح بقرار من المالك: هويات، أدوار حقيقية لصفحة الفريق، وربط الفوترة بمزود حقيقي.

---

## قواعد الخارطة

1. لا تبدأ مرحلة قبل اكتمال بوابة خروج سابقتها.
2. كل مهمة تُنفّذ بمقياسها المرجعي المذكور — لا «تحسينات جانبية» غير موثقة.
3. إضافة مهمة جديدة = إدراجها هنا بمرجعها قبل التنفيذ.
4. إنجاز مرحلة يُعلن في CHANGELOG بمعرّفات المتطلبات المحققة.
