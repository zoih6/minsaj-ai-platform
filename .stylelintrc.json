{
  "comment": [
    "W-DS G-7 token lint — stylelint half (VISUAL-QA-CHECKLIST.md §4).",
    "WARN MODE by design (Phase 0): every rule reports as a warning so the lint measures",
    "token-adoption progress (SPC-01 / SUR-02 / SUR-03 / TYP-01 census) without blocking builds.",
    "Phase 6 flips severity to error per the approved spec. What is checked here:",
    "R-SPACE-1 no raw length literals in spacing properties;",
    "R-SURF-2 border-radius only via the radius token map / 50% / 0 / 999px;",
    "R-SURF-3 box-shadow only via --u-shadow-* tokens / none;",
    "R-TYPE-1 font-size only via the type ramp tokens (raw sizes warn — census).",
    "The context-aware G-7 checks (Arabic tracking pairing, globals.css selector trend,",
    "primitive-class census) live in scripts/token-lint.mjs — run both: npm run lint:css && npm run lint:tokens."
  ],
  "rules": {
    "at-rule-no-unknown": [true, {
      "ignoreAtRules": [
        "tailwindcss", "theme", "custom-variant", "source", "plugin",
        "config", "utility", "variant", "apply", "reference"
      ]
    }, { "severity": "warning" }],
    "declaration-property-value-disallowed-list": [
      {
        "/^(padding|margin|gap)/": ["/-?\\d*\\.?\\d+(px|rem|em)\\b/"]
      },
      { "severity": "warning" }
    ],
    "declaration-property-value-allowed-list": [
      {
        "border-radius": [
          "/^var\\(--u-radius/",
          "/^(var\\([^)]+\\)|50%|0|999px)(\\s+(var\\([^)]+\\)|50%|0|999px))*$/",
          "inherit"
        ],
        "box-shadow": [
          "/^var\\(--u-shadow/",
          "/^var\\([^)]+\\)(,\\s*var\\([^)]+\\))*$/",
          "none",
          "inherit"
        ],
        "font-size": [
          "/^var\\(--(mj|u)-text/",
          "/^var\\([^)]+\\)$/",
          "0",
          "inherit",
          "larger",
          "smaller"
        ]
      },
      { "severity": "warning" }
    ]
  }
}
