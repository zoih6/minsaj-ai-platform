# CHANGELOG — سجل التغييرات
## منسج · Minsaj AI Platform

> صيغة السجل: معكوس زمنيًا (الأحدث أعلى). كل قسم: Added / Changed / Fixed / Docs / Verification، بمعرّفات المتطلبات (FR) والعيوب (KI) المتأثرة.

## [2026-09-22 · W-DS §7.5 · وفاء الشرط] — اختبار عدم حجب الـ Mobile Dock منفذ وموثق: Phase 3 مقفلة وPhase 4 مفتوحة

### Added
- **مسبار حجب الدوك** (`scripts/visual-qa/dock-occlusion-probe.mjs`): التمرير المرحلي إلى القاع المطلق (يطلق كل مراقبي scroll-reveal) ثم قياس تقاطع كل عنصر تفاعلي في المحتوى مع مستطيل الدوك + اختبار نقر في مركز منطقة التقاطع + تسجيل آخر عنصر تفاعلي وفجوته عن حافة الدوك.
- **أدلة الشرط** في `evidence/baselines/phase3/`: `dock-occlusion-probe.json` (98 خلية كاملة التفصيل) + لقطات القاع المطلق للأقسام السبعة + home/chat.

### Verification
- **98/98 خلية PASS**: 19 مسار × 390/375/360 (فاتح ar RTL) · أقسام الإعدادات السبعة كاملة × ثلاثة أعربة · داكن (12 خلية) · en LTR (11 خلية) — **صفر عناصر محجوبة · صفر أخطاء · كل الخلايا عند القاع المطلق (scrollY = maxScroll) · أصغر فجوة 21.9px** (library@375) — أكبر الفجج 1215px.
- ملاحظتان مسجلتان بأمانة (ليستا فشل حجب): ذيل غير تفاعلي طويل أسفل المعرفة (~1215px — يُرفع لسياق D-3/UNC-01 المفتوح) والفريق (~472px — لأنماط Phase 4+).
- **الحكم: الشرط وُفي — Phase 3 مغلقة وPhase 4 (الأنماط) مصرّح بها وفق §7.5.**

## [2026-09-22 · W-DS §7.5 · تسجيل اعتماد] — بوابة خروج Phase 3 مُعتمدة باعتماد مشروط + تصريح Phase 4 (الأنماط)

### Docs
- **§7.5 في الوثيقة الرئيسية:** قرار المالك مسجلًا — PHASE 3 — APPROVED WITH CONDITIONS: إقرار بتحقق أهداف NAV-01..05 كاملة (رأس 56px صفًا واحدًا · إلغاء صف البحث الثاني · الميزانية ضمن الحدود · دوك عائم واحد · درج T1–T4 · صفر تجاوز أفقي · صفر انحدار لمس · RTL/LTR وLight/Dark والأحجام الصغيرة ناجحة).
- **شرط الإغلاق النهائي:** التحقق من أن الـ Mobile Dock لا يحجب آخر محتوى قابل للتفاعل — خصوصًا الإعدادات وبقية صفحات الهاتف — مع تسجيل نتيجة اختبار التمرير.
- **انضباط النطاق:** اختلاف أنماط المحتوى الداخلي تبقى ضمن Phase 4 — Page Patterns — ولا تُعد فشلًا في Phase 3.
- **التصريح:** Phase 4 مصرّح بها فور توثيق اختبار عدم الحجب · W-4 تبقى محجوبة · D-3/D-4 مفتوحان ولا يحجبان.
- تحديث المصفوفة الزمنية: roadmap (DS-5..10 + ترويسة W-DS) · AGENT.md (§0 + §10).

## [2026-09-22 · W-DS Phase 3 · الكروم] — حل NAV-01..05 بقياسات البوابتين

### Added
- **أساس Phase 3 المجمد:** `evidence/baselines/phase3/` (capture-aggregate 100 خلية + g7-baseline + مسبارا nav-probe قبل/بعد + drawer-390 + لقطات بعد) مع README بسجل الانحراف الكامل: 401 فرق حقل كلها مصنفة إلى حقول الكروم حصرًا، صفر انحدارات.
- **مسبار NAV** (`scripts/visual-qa/nav-probe.mjs` + `nav-edge-probe.mjs`): يقيس فئات عيوب الكروم مباشرة — عمارة الرأس (ارتفاعات + صف ثانٍ) · الميزانية العمودية · تكرار الهوية · لهجات الدوك (بصمة هندسية لكل مسار) · سجلات درج الطبقات على النص المُصيَّر — قبلًا على بناء Phase 2 (worktree @edc4c01) وبعدًا على بناء Phase 3.

### Changed
- **NAV-01/NAV-03 محلولان:** رأس واحد = صف مفرد 56px على كل مسارات /app/* (كان 107/65px لهجتين) — `[menu][mark] … [⌘K][theme][lang][bell][avatar]`، خلفية شبه مصمتة 0.95 (قرار D-1) بلا عنوان صفحة (الهوية تعلنها h1 الخاصة بالمسار — R-NAV-3) · حقل البحث المضمّن مات ومعه صينية البحث ذات الصفين · زر ⌘K أيقونة <768 وحقل مضمن ≥220px فوقها · الكلمة المطبوعة تظهر ≥768 فقط (حارس <370px موثق للغات). القياس: headerHeights [109,65] ← [56] · صفوف ثانية 6 ← 0 · تكرار هوية 4 ← 0.
- **NAV-04 محلول:** عامل إرساء عائم واحد لكل المسارات (كان شريطًا بعرض الشاشة على القياسية ودوكًا زجاجيًا على focus-canvas) — إنست 16px · 64px ارتفاعًا (8+48+8) · نصف بطاقة 16 · عناصر بنصف حقل 12 · ظل توكن واحد + خط شعرة واحد (سقف D-4) · نشط = أساسي + حبة ناعمة + مؤشر 3px. تجاوزات focus-canvas الأربعة عشر للكروم حُذفت (بقي القماش واحتياطي المحتوى 112px بتعليق موثق) · قواعد interaction.css §2a/2b أُحيلت بتعليقات إحالة (المالك واحد لكل سطح).
- **NAV-05 محلول:** هرمية الدرج الرباعية — T1 الخدمات: label-m 13px/600 حبر + أيقونة 18 في رقاقة 28px + صف 48px + نشط حبة أصلية ومؤشر 3px · T2/T3: body-s 13.5px/500 + أيقونة 16 عارية + صف 44px + نشط صبغة ناعمة · T4 «مساحتي»: مسمّاة (كانت بلا تسمية) ومثبّتة فوق صف الملف خارج التمرير · التشغيل: فاصل شعرة + طيّ `<details>` خلف إفصاح «التشغيل» على نطاق الهاتف (الدرج يتجاوز 80vh) ومفتوح معطّل على نطاقات الشريط. القياس: سجل واحد 13.5px/550/44px ← سجلان مرجحان (13px/600/48px · 13.5px/500/44px) · 19 وجهة محفوظة.
- **NAV-02 محلول:** الميزانية العمودية مُفرضة — 56+66=122px = **14.5% موحدًا @390** (كانت 15.5–21.2%) و**13.1% @430** (البوابة ≤14.5%) عبر الخلايا الخمس والتسعين كلها · الاحتياطي --mj-tabbar-reserve ثابت 80px + safe-area.
- تنظيفات تابعة للكروم: موضع لوحة الإشعارات 62 ← 64px (يطابق رأس 56px) · شارة العرض الميتة أُزيلت من الترميز وقاعدتها من responsive.css · تعليق z-index محدّث.

### Docs
- المصفوفة: NAV-01..05 = resolved-in-phase-3 بالقياسات · NAV-06 = partially-addressed-in-phase-3 (نصف الرأس سُلّم مع NAV-01؛ نطاق المجموعات يبقى مفتوحًا) · §6 في الوثيقة الرئيسية · roadmap DS-5..10 · AGENT.md §0/§10.

### Verification
- Gate A: بناء إنتاجي نظيف · ESLint نظيف · tsc صفر أخطاء · stylelint صفر أخطاء · **صفر تمرير أفقي ×100 خلية + مسابر 360/375/980-coarse** · صفر انحدارات tapUnder44.
- Gate B (نطاق Phase 3): **B-5 كاملة القياس** (chromeRatio موحد تحت البوابة · صف واحد · لهجة إرساء واحدة · هوية معلنة مرة) · B-1/B-3/B-4 ثابتة بصفر فروق (title/cards/nesting) · B-9 جزئية موثقة (إعادة التوقيت نفسها لـ Phase 4).
- G-7: مسافات 198←192 · ظلال خام 63←59 · مراجع قديمة 1113←1102 · صفر دون 10px · صفر تتبّع غير مقترن · الأصناف مجمدة عند 67 (الطبقات عبر data-tier لا أصناف جديدة).
- سجل الانحراف: `evidence/baselines/phase3/README.md` — **بانتظار اعتماد بوابة خروج Phase 3 من المالك قبل فتح Phase 4 (الأنماط).**

## [2026-09-22 · W-DS §7.4 · تسجيل اعتماد] — بوابة خروج Phase 2 مُعتمدة + تصريح بدء Phase 3 (الكروم)

### Docs
- **§7.4 (الوثيقة الرئيسية):** رسالة المالك «اعتمد Phase 2 وابدأ Phase 3» سُجلت: حزمة أدلة Phase 2 (أساس مجمّد phase2 + 198 فرقًا مصنفة + مسبارا SUR-01 قبل/بعد + Gate A نظيف + B-4 كاملة القياس) مُعتبرة مُجتازة لبوابة الخروج، وPhase 3 (الكروم) مصرّح بها فورًا: رأس صف مفرد 56px شبه مصمت (NAV-01/03) + عامل إرساء عائم واحد (NAV-04) + هرمية الدرج T1–T4 (NAV-05) + فرض الميزانية العمودية ≤15.5% @390 (NAV-02).
- القيود الحاملة تُعاد صياغتها: أسس phase0/1/2 لا تُستبدل · لا حل يُعلن قبل Gate A+B بقياسات جديدة · لا Phase 4 قبل اعتماد بوابة خروج Phase 3 · W-4 محجوبة · D-3/D-4 مفتوحان دون حجب · إعادة توقيت انتقال المسارات (240ms/4px) تبقى في نطاق Phase 4.
- roadmap (W-DS) وAGENT.md (§0/§10) حُدّثت في الكوميت نفسه وفق قاعدة الوثائق-مع-الكود.

## [2026-09-22 · W-DS Phase 2 · السطوح] — حل SUR-01 (P0) + HIE-02 + HIE-03 بقياسات البوابتين

### Added
- **أساس Phase 2 المجمد:** `evidence/baselines/phase2/` (capture-aggregate 100 خلية + g7-baseline + مسبارا sur01-probe قبل/بعد + لقطات بعد) مع README بسجل الانحراف الكامل: 198 فرق حقل كلها مصنفة، صفر انحدارات.
- **مسبار SUR-01** (`scripts/visual-qa/sur01-probe.mjs`): يقيس الحاويات الشبيهة بالبطاقات المتداخلة (فئة العيب الحقيقية) منفصلة عن عناصر التحكم داخل سطحها (الشجرة المُجازة surface > flat-group > controls) — قياس before على بناء Phase 1 (worktree) وبعد على بناء Phase 2.

### Changed
- **SUR-01 (P0) محلول:** 47 قاعدة حاوية داخلية في workbench سُطّحت إلى مجموعات مسطحة (border:0 · --u-surface-subtle · --u-radius-field) وناقلات الحالة إلى صبغات الخلفية · u2-workbench__body من بطاقة-في-بطاقة إلى عمود · نصف workbench إلى درجة البطاقة (16px) · سلسلة هوم العمق-3 فُكّت (task-card غلاف شفاف وظلال submit الخام حُذفت). القياس: **maxDepth>1 من 10 خلايا ← 0/100** (هوم 3←1) · **حاويات-في-حاويات بدرجة البطاقة 10←0 لكل عرض** (مسبار before/after).
- **HIE-02 محلول:** الإعدادات على mj-section ← (mj-surface | mj-data-list) — نصف القطر {12px:5} ← {16+12} حسب خريطة المستويات · 4/5 بطاقات بلا ظل ← 0 · حشو 19px خام ← --mj-pad-card · 44 تعريف settings-* ← 0 · صندوق الرفع المتقطع أُزيل.
- **HIE-03 محلول:** المكتبات الست (المعرفة/الوكلاء/التدفقات/التشغيلات/الفريق/المشاريع) + المزودون وسجل التدقيق على صفوف mj-data-list مقسمة في حاوية واحدة — البطاقات للإحصاءات والملخصات فقط · مشاريع: الصفوف افتراضية والشبكة خيار المستخدم · تشغيلات: الصف كامل رابط. القياس: بطاقات @390 المعرفة 9←6 · الوكلاء/التدفقات 10←8 · المشاريع 7←5.
- **أرضية اللمس لصفوف القوائم:** layout.css §11 من 40px إلى --u-touch (44) لأزرار/روابط/قوائم صفوف DataList على نطاق الهاتف + توسيع كتلة الأرضية ≤768 للفئات الجديدة (كانت تشير لفئات محذوفة) — القياس: صفر انحدارات tapUnder44 وتحسينات (المعرفة 5←1 · التشغيلات 7←2).

### Docs
- المصفوفة: HIE-02/SUR-01/HIE-03 = resolved-in-phase-2 بالقياسات (عمودا status وmeasured_value) · §6 في الوثيقة الرئيسية (Phase 2 منفذة ومقاسة) · roadmap DS-5..10 · AGENT.md §0/§10.

### Verification
- Gate A: بناء إنتاجي نظيف · ESLint نظيف · stylelint صفر أخطاء · **صفر تمرير أفقي ×100 خلية** · صفر فروق كروم (topbar/bottomNav/chromeRatio/titleSize).
- Gate B (نطاق Phase 2): B-4 كاملة (عمق ≤1 ×100 · الأنصاف ⊆ الخريطة · صفوف لا أكوام) · B-2/B-6/B-8 جزئية موثقة في سجل الانحراف.
- G-7: مسافات 210←198 · ظلال خام 72←63 · مراجع توكنز قديمة 1223←1113 · محددات globals 1766←1615 · صفر دون 10px · صفر تتبّع عربي غير مقترن · صفر أصناف مكوّنات جديدة خارج طبقة البدائع.
- المصفوفة وسجل الانحراف: `evidence/baselines/phase2/README.md` — **بانتظار اعتماد بوابة خروج Phase 2 من المالك قبل فتح Phase 3 (الكروم).**

## [2026-09-22 · W-DS §7.3 · تسجيل اعتماد] — بوابة خروج Phase 1 مُعتمدة + تصريح بدء Phase 2 (السطوح)

### Docs
- **§7.3 (الوثيقة الرئيسية):** رسالة المالك «نعم ابدأ phase 2» سُجلت: حزمة أدلة Phase 1 (أساس مجمّد + 238 فرقًا مصنفة + Gate A نظيف + Gate B بالنطاق) مُعتبرة مُجتازة لبوابة الخروج، وPhase 2 مصرّح بها فورًا: إعدادات/تشغيل على `mj-surface` (HIE-02) + مضاد التداخل workbench/gateway (SUR-01) + `mj-data-list` للسجلات (HIE-03).
- القيود الحاملة تعاد صراغة: أساسا phase0/phase1 يُصانان، لا حل يُعلن قبل Gate A+B بقياسات جديدة، لا Phase 3 قبل اعتماد بوابة خروج Phase 2، W-4 محجوبة، D-3/D-4 مفتوحان.
- roadmap (DS-5..10) + AGENT.md (§0 الحالة + §10 تقسيم الجلسة القادمة) محدثة في نفس الكوميت.

## [2026-09-22 · W-DS Phase 1 · الأسس] — اعتماد Wave 1: توكنز §10 + حل TYP-02/TYP-03 + بدء SPC-01 (مقاسًا ببوابتتي A وB)

### Added
- **أساس Phase 1 المجمد:** `evidence/baselines/phase1/` (capture-aggregate 100 خلية + g7-baseline) مع README بسجل الانحراف الكامل: 238 فرق حقل كلها مصنفة إلى فئات معتمدة (§10/§R-TYPE-2/R-RTL-2/R-SURF-2) + سجل تحولات القيم لكل توكن.
- **ترقية G-7 (فحوص أقوى):** اقتران التتبّع العربي لكل محدد (قانوني فقط: نطاق `[lang="en"]` أو صنف لاتيني صريح أو تجاوز rtl/ar مرافق — مقياس `unpairedTracking` هدفه صفر) + إحصاء مراجع الأسماء القديمة `legacyTokenReferences` (1223 عند إغلاق Phase 1 — يتراجع مع Phases 2-5 حتى حذف الأسماء البديلة) + حفظ أرقام الأسطر عند تجاهل التعليقات.
- **تتبّع لاتيني قانوني للمنحدر:** قاعدتان فقط تحت نطاق `[lang="en"]` — display ‎-0.02em وh1 ‎-0.01em (عمود Tracking (Latin) في جدول المنحدر).

### Changed
- **اعتماد §10 كاملًا (DESIGN-TOKENS):** الأسماء القانونية تحمل قيم المواصفة — منحدر الـ11 مستوى (h1: clamp(26, 1.6vw+20, **34px**) — سقف 40px أُزيل؛ h2 إلى 24px؛ body-m 15-16px هو النص الافتراضي؛ hero→display) · مقياس المسافات التسعي مصدر كل فجوة (--mj-gap-* أسماء بديلة) · خريطة الأنصاف (control/field/card/overlay قانونية؛ البدائيات mj-* تستهلكها مباشرة) · أسطر قاعدية عربية (R-RTL-2: 1.25/1.35/1.35/1.7/1.6) · **--u-alert** هو توكن الانتباه الوحيد.
- **TYP-02 (bug مؤكد) محلول:** 23 اختصار `font:` دون 10px (mono تقنية 7-9px) رُفعت — الحرة إلى `--mj-text-code` وشارات الدوائر إلى أرضية R-TYPE-2 (10/11px). القياس: **23 ← 0**.
- **TYP-03 (bug مؤكد) محلول:** 30 تتبّعًا غير مقترن حُذف (تسميات عربية بتتبّع يتمزق به الوصل). القياس: **37 ← 0 غير مقترن** (6 قانونية فقط).
- **SPC-01 بدأ:** 398 تعريفًا حُوّل آليًا بمطابقة تامة (قيم غير مطابقة تُركت لمراحل إعادة البناء). القياس: padding خام **155 ← 141** · مسافات **243 ← 210** · أنصاف **15 ← 12** · أحجام **29 ← 26**.

### Fixed
- **INT-03 (توكن التنبيه):** نقطة الانتباه في الشل تنتقل من الوردي #e75f9d إلى `--u-alert` #ef4444 (توافق 1.4.11: 3.9:1 مقابل 2.9:1 سابقًا) — حذف --u-shell-alert نهائيًا (صفر مستهلكين).
- **R-SURF-2 (هيرو الصفحة الرئيسية):** نصف قطر clamp(22-30px) ← `--u-radius-card` (16px) — حذف --u-radius-xl (قائمة المرفوضات §13).

### Docs
- §7.2 (اعتماد بدء Phase 1 + قيودها المُلزِمة) في الوثيقة الرئيسية · سجل اعتماد §10 في DESIGN-TOKENS · ترقية G-7 موثقة في VISUAL-QA-CHECKLIST · المصفوفة: TYP-02/TYP-03 محلولان بقياسات، SPC-01 قيد التقدم · roadmap/AGENT/worklog (Task ID 11).

### Verification
- Gate A: بناء إنتاجي نظيف · ESLint نظيف · stylelint صفر أخطاء (1060 تحذيرًا وضع تحذير مقابل 1456 في Phase 0) · **صفر تمرير أفقي ×100 خلية** · tapUnder44/bottomNavH بلا تغيير ×100.
- Gate B (نطاق Phase 1): B-3 (صفر دون 10px، صفر تتبّع غير مقترن، منحدر مقاس) · B-4 جزئي (هيستوغرام الأنصاف ⊆ الخريطة) · B-7 (توكن تنبيه واحد، بلا صبغات جديدة) · B-2 جزئي (141 متناقصة، صفر جديد).
- المصفوفة كاملة وسجل الانحراف: `evidence/baselines/phase1/README.md` — **بانتظار اعتماد بوابة خروج Phase 1 من المالك قبل فتح Phase 2.**

---

## [2026-09-22 · W-DS Phase 0 · الحراس] — توكن-لينت G-7 + أساس قياس مجمّد + طبقة أسماء توكنز بديلة (صفر تغيير مرئي مثبت)

### Added
- **طبقة الأسماء البديلة (alias layer):** إعلان نطاق التوكنز المعتمد كاملًا في `layout.css`/`foundations.css` — مقياس المسافات `--mj-space-*` (9 درجات) · مستويات المنحدر الجديدة `--mj-text-display/body-l/m/s/label-m/s/code` · سلم الحاويات `--mj-container-narrow/prose/wide/full` · سلم ارتفاعات التحكم `--mj-control-dense/compact/prominent` · الأنصاف الموضعية `--u-radius-control/field/card/overlay` (مرادفات للقيم القائمة حرفيًا) · `--u-alert` (توكن الانتباه الوحيد) · `--u-shadow-none` · `--u-motion-duration-route: 240ms`. إضافي بحت: لا مستهلك حُوّل ولا قيمة قديمة غُيّرت — التبني في Phase 1.
- **G-7 توكن-لينت بوضع التحذير:** `.stylelintrc.json` (stylelint 16، متوافق مع Tailwind 4، كل القواعد تحذير) + `scripts/token-lint.mjs` (بدون تبعيات؛ 7 فحوص سياقية مع `file:line` ووضع `--compare`) + أوامر `npm run lint:css` / `lint:tokens`. أساس مجمّد: **155 قيمة padding خام مميزة (تكافؤ منهجي 196 مقابل 186 في التدقيق) · 15 نصف قطر غير معلن · 72 ظل خام · 29 حجم خط خام · 23 إعلان نص <10px (منها ما يختبئ في اختصار `font:` وفات التدقيق) · 37 مرشح تتبّع عربي · 1766 محددًا في globals.css · 67 صنف مكون مجمّد**.
- **أداة QA بصري داخل المستودع:** `scripts/visual-qa/capture.mjs` — نفس دالة قياس التدقيق الجنائي (كل رقم بمنتقيه)، 20 مسارًا × 5 نقاط عرض + مسبار الدرج + مراجع LTR، تجميع بصيغة أساس التدقيق، وضع `--compare` لتقرير الانحراف الصفري، صمود ضد انهيار Chromium.
- **CI:** `.github/workflows/visual-qa-guards.yml` — eslint إلزامي + تقارير G-7 بالدلتا مقابل الأساس (لا يحمرّ قبل Phase 6).
- **أساس Phase 0 المجمد:** `docs/03-design/reconstruction/evidence/baselines/phase0/` (capture-aggregate + g7-baseline + drawer-390 + README بالمنهج والأرقام).

### Verification
- **صفر انحراف بصري مثبت بالقياس:** بناء إنتاجي → التقاط 100 خلية → مقارنة مع أساس التدقيق: **100 خلية، 0 اختلاف حقول، VERDICT: IDENTICAL** (topbarH/chromeRatio/cards/nestedCards/radii/fontSizes/h1/… كلها مطابقة، docHeight ضمن ±4px) — طبقة الأسماء البديلة لم تحرّك بكسلًا مرئيًا.
- **Gate A للتغيير:** بناء نظيف (~29 مسارًا) · ESLint نظيف · stylelint 1456 تحذيرًا / 0 أخطاء (وضع التحذير مقصود) · صفر تمرير أفقي ×100 خلية.
- إعادة إنتاج نتائج التدقيق على البناء الحالي: الكروم 15.5–21% عند 390 · 5 أحجام h1 (37/34/33/28/23px) — أي لا مشكلة اعتُبرت محلولة؛ هذا الأساس هو نقطة انطلاق القياس لكل الموجات.

---

## [2026-09-22 · تسجيل اعتماد W-DS] — المالك يعتمد التشخيص والمواصفة وخطة التنفيذ

### Docs
- **الاعتماد وُثّق في §7.1 من `docs/03-design/reconstruction/DESIGN-SYSTEM-RECONSTRUCTION.md`** (الحالة DRAFT ← APPROVED): المواصفة المعتمدة مُلزِمة لكل تنفيذ لاحق؛ أي تعديل عليها يمر ببروتوكول قبول التوكنز (اختبار الأسئلة الستة + توقيع المالك).
- **الشروط المُلزِمة من نص الاعتماد:** يُسمح ببدء Phase 0 فور تسجيل الاعتماد · لا تُعتبر أي مشكلة محلولة قبل اجتياز Gate A وGate B وتسجيل القياسات الجديدة · W-4 تبقى محجوبة حتى شروط خروج W-DS.
- **قرارات D-1..D-5 (موقفها بعد الاعتماد):** D-1 شفافية الهيدر — الافتراضي (شبه مصمت 0.95) · D-2 تنقل التابلت — الافتراضي (شريط جانبي موسّع بمُسميات) · D-3 سياق UNC-01 — يبقى مفتوحًا للمالك · D-4 وزن الشريط السفلي — مفتوح كتفضيل مع قاعدة سقف الوزن العاملة · D-5 ترتيب التنفيذ — مؤكد.
- roadmap.md وAGENT.md محدثة لانعكاس الاعتماد وبدء التنفيذ.

---

## [2026-09-22 · جلسة W-DS · الكوميت 1] — تدقيق جنائي لواجهة/تجربة + وثائق إعادة بناء نظام التصميم (وثائق فقط، لا كود)

### Added
- **مجموعة وثائق W-DS كاملة** في `docs/03-design/reconstruction/` (بالإنجليزية بقرار المالك — للاستهلاك من وكلاء AI والمطورين): DESIGN-SYSTEM-RECONSTRUCTION.md (الرئيسية) + DESIGN-PRINCIPLES + DESIGN-TOKENS + COMPONENT-INVENTORY + NAVIGATION-ARCHITECTURE + RESPONSIVE-ARCHITECTURE + PAGE-PATTERNS-AND-MOTION + VISUAL-QA-CHECKLIST + USAGE.md (موجّه قراءة الوكلاء بنمط OpenDesign).
- **مصفوفة UI-UX-AUDIT-MATRIX.csv:** 37 نتيجة مُصنفة (confirmed-bug / design-system-inconsistency / ux-problem / subjective-refinement / unconfirmed-pending-owner) بدرجات P0-P3، كل نتيجة مربوطة بدليل مقاس ومحدد DOM.
- **حزمة أدلة evidence/:** قياسات 100 التقاط (20 مسارًا × 5 نقاط عرض: 390/430/768/1024/1440) + مسبار وضع سطح-المكتب-على-الهاتف (980px) + قياسات الدرج/الهيدر/المعرفة + سجلات الطبقة الإدراكية (VLM بضوابط: إدراك فقط، مربوط بالقياس) + اللقطات.

### Docs
- roadmap.md: فتح مرحلة **W-DS إعادة بناء نظام التصميم** قبل W-4 (بقرار المالك: لا W-4 قبل اعتماد W-DS).
- 00-INDEX.md و design-system.md و AGENT.md: إشارات للمجموعة الجديدة.

### Verification
- **القاعدة الجديدة مطبقة:** لا يُنكر ادعاء إدراكي بمجرد DOM، ولا يُقبل حكم إدراكي بلا قياس. ملاحظات المالك الـ16 كلها مُحققة ومُصنفة (§3.2 من الوثيقة الرئيسية).
- **أبرز المؤكدات المقاسة:** هيدران مختلفان (107px مقابل 65px عند 390) · الكروم 21% من الشاشة · 93% بطاقات متداخلة في المساحات النطاقية · 186 قيمة padding خام مقابل 10 توكنز · 5 أحجام h1 عند 390 · نص 7-8px حي في المنتج · وضع سطح المكتب على الهاتف يعرض نصًا فعليًا ~6.5px (RES-01) · الإعدادات جلد مختلف (12px بلا ظل مقابل 16px مظلل).
- **البوابات الهندسية سليمة ولم تُنقض:** صفر تمرير أفقي ×100 لقطة، أهداف لمس ≥44px — الجودة الإدراكية طبقة إضافية لا بديل.
- لا تغيير كود إطلاقًا في هذه الجلسة (مرحلة وثائق).

---

---

## [2026-09-21 · جلسة W-3.5 · الكوميت 1] — صقل إيقاع الظهور (P-1) + تشخيص بصري شامل بتحقق هندسي

### Fixed
- **P-1a · بطل الهبوط كان رهين JS:** عناصر البطل الخمسة (الشارة/العنوان/الوصف/الأزرار/مؤشرات الثقة) حملت `reveal-on-scroll` فبدأت `opacity:0` ولا تظهر إلا بعد `window load + 2rAF` — على اتصال بطيء يعني ذلك **صفحة بيضاء لثوانٍ**. أُزيل الاعتماد: البطل يُعرض فورًا حتى قبل الترطيب. (`site-hero.tsx`)
- **P-1b · المحتوى كان يطارد التمرير:** كاشف التقاطع كان `-8%` هامش سفليًا بعتبة 15% — أي أن العنصر يدخل الشاشة شفافًا ثم يقفز ظاهرًا متأخرًا. الآن `+15%` هامشًا (يُكشف قبل دخوله الشاشة) بعتبة 0. (`reveal-fx.tsx`)
- **P-1c · حركة استعراضية:** إزاحة 30px/480ms صارت 14px/320ms — همسة لا موكب. (`marketing.css`)

### Docs
- roadmap.md: فتح مرحلة **W-3.5 صقل الواجهات** بقرار المالك («الواجهات حاليًا خاربة») مع جدول مهام P-1..P-5.
- **نتيجة التشخيص الهندسي الموثقة (القاعدة: تحقق بايت-آمن قبل تصديق أي ادعاء بصري):** 9 من 12 ادعاءً من مراجعات VLM ثبت أنها هلوسات (مؤشر التنقل الجانبي، مزج «workspace» اللاتيني، فشل تباين placeholder، «فراغ 65%» في الرئيسية، بطاقات راديو غير موجودة في DOM، أهداف لمس ضئيلة 64-94px فعلية، فشل تباين الداكن 0/89 عنصرًا، خلفيات بيضاء في الداكن، «عدم اتساق» المساحات الخمس المتطابقة حرفيًا على u2-workbench). البقية المفتوحة: P-5 (هوية بصرية للهبوط) بقرار المالك.

### Verification
- build نظيف (66/66 صفحة) · lint نظيف · tsc صفر أخطاء.
- DOM: البطل يظهر فورًا (0 عناصر مخفية) في ar وen · العنصر تحت حافة الشاشة يبلغ 63% ظهورًا قبل دخوله (نمط ما-بعد) · 390px بلا تمرير أفقي · 18 هدفًا تحت الطية يحتفظ بالكشف اللطيف.

---

---

## [2026-09-21 · جلسة W-3 · الكوميت 3] — هبوط شريحة استكشف + اكتمال W-3: ست مساحات نطاقية

### Added
- **شريحة استكشف الكاملة (6 مراحل):** عقود `contracts/services/explore.ts` + قاموس `explore-content` ثنائي + مخطط معرفة مزروع (`mock-api/services/explore`: موضوعان، عقد وروابط مسماّة، قاعدة العمق المحدودة) + `src/features/explore/` + سطح تحق `/preview/service-explore`.

### Changed
- **قلب السجل:** `explore: prototype_service_workspace/foundation → domain_workspace/implemented` — به يكتمل W-3: **الخدمات السبع كلها الآن مساحات نطاقية عبر السجل** (تعلم/ابحث/أنشئ/برمج/حلّل/استكشف) والبوابة المشتركة ask على `/app/chat`.

### Docs
- page-specs: **D-6** + IA §6 تحديث السجل.

### Verification
- المسار الموجّه مختبر تفاعليًا: بذرة → خريطة (3 عقد، روابط مسماّة) → تفاصيل عقدة → رحلة (تقليم + استعادة) → نقطة توقف → تأكيد + حفظ + نقل + استئناف إلى نفس المرحلة.
- build نظيف (66 صفحة) · lint نظيف · tsc صفر أخطاء · 390px بلا تمرير أفقي · ثيمان × اتجاهين مفيحصان.

---

## [2026-09-21 · جلسة W-3 · الكوميت 2] — هبوط شريحة حلّل: قلب الخدمة إلى مساحة نطاقية (U2.5)

### Added
- **شريحة حلّل الكاملة (8 مراحل):** عقود `contracts/services/analyze.ts` + قاموس `analyze-content` ثنائي + قواعد `mock-api/services/analyze` (فقرتان، قوالب خطط بالعملية، حساب حتمي، 4 فحوص تطابق) + `src/features/analyze/`.
- **سطح تحقق** `/preview/service-analyze` عبر `SliceHarness` + إيصال شريحة في `sliceReceipt` بالسجل.

### Changed
- **قلب السجل:** `analyze: prototype_service_workspace/foundation → domain_workspace/implemented` — `/app/analyze` يعرض الآن الحساب المحلي الحتمي من المنسب إلى الملخص. كتلة حلّل تدخل مخزن العرض.

### Docs
- page-specs: **D-5** موصفة شريحة حلّل الكاملة + IA §6 تحديث حالة السجل.

### Verification
- المسار الموجّه كاملًا مختبر تفاعليًا: منسب → ملف تعريفي → سؤال بافتراض → خطة → حساب 5 خطوات بنقرة واحدة → نتيجة (رقم رئيس 117، −0.8تلائمتباقًا مع القاعدة الموثقة: تنظيف قفرة واحدة) → تحقق → ملخص · حارس التأكيد يتطلب إقرار الفحوص فردى فردى · الحفظ والاستئناف والنقل إلى أنشئ مختبرة.
- build نظيف · lint نظيف · tsc صفر أخطاء · 390px بلا تمرير أفقي · ثيمان × اتجاهين مفيحصان.

---

## [2026-09-21 · جلسة W-3 · الكوميت 1] — هبوط شريحة برمج: قلب الخدمة إلى مساحة نطاقية (U2.4)

### Added
- **شريحة برمج الكاملة (7 مراحل):** عقود `contracts/services/code.ts` (حالة الجلسة المخططة) + قاموس `code-content` ثنائي بالتكافؤ + قواعد `mock-api/services/code` (مشروعان تجريبيان، 8 خطوات بالقوالب، قواعد المخاطر والفحوص) + `src/features/code/` (مسار + مساحة عمل + 7 أسطح + آلة حالات بحدود ثقة).
- **سطح تحقق الشريحة** `/preview/service-code` عبر `SliceHarness` مشترك (يركّب مسار الخدمة الفعلي مع مفاتيح السيناريو واللغة).
- **إيصال الشريحة في السجل:** حقلا `sliceVerificationSurface` + `sliceReceipt` في `ServiceRegistryEntry` مع أدلة التحق (نمط السجل الحالي — الوثيقة التي طلبتها W-3).

### Changed
- **قلب السجل:** `code: prototype_service_workspace/foundation → domain_workspace/implemented` — المسار `/app/code` يعرض الآن مراجعة التغيير المقترح (نطاق → خطة → اقتراح → فرق → نسخة عمل → فحوص → إيصال أثر). البوابة النموذجية تبقى حصريًا للبوابة المشتركة ask على `/app/chat`.
- **قاعدة الحفظ:** `ServiceDomainBlock` يقبل كتلة برمج المخططة (استئناف موثق بعد إعادة التحميل).

### Docs
- page-specs: **D-4** موصفة شريحة برمج الكاملة + IA §3 صفوف 35–37 + IA §6 تحديث حالة السجل + ملاحظة إيصالات الشرائح.

### Verification
- المسار الموجّه كاملًا مختبر تفاعليًا: نطاق → خطة → اقتراح → فرق (بمراجعة الملفين) → نسخة عمل → 4 فحوص → إيصال (ثبت الأرقام: 2 ملفات، +27، −3) · الحارس يرفض المتابعة بلا مراجعة · الوضع السريع يقفز مراجعة الخطة في اللغتين · النقل يؤكّد إلى حلّل · الحفظ يدخل كتلة برمج في المخزن والاستئناف يعود للمرحلة المحفوطة.
- build نظيف (62 صفحة) · lint نظيف · tsc صفر أخطاء · 390px بلا تمرير أفقي · ثيمان × اتجاهين مفيحصان بصريًا.

---

## [2026-09-21 · جلسة المعالجة 4] — إغلاق KI-4: تثبيت اصطلاح ask/chat

### Docs
- **KI-4 (🟡) مغلق:** اعتماد الاصطلاح الرسمي «الهوية `ask` · المسار `/chat`» وتوثيقه في IA §3 (مقدمة جدول المسارات) و§6 (سجل الخدمة) — الهوية للسجل والمحتوى، والمسار يطابق URL وعنصر التنقل. لا تغيير كود مطلوب.
- AGENT.md §3 علّق الاصطلاح عند صف chat/ في خريطة الملفات.

### Verification
- فحص Grep: الهوية ask في السجل والمحتوى، والمسار/التنقل chat — كل موضع يطابق الاصطلاح المعتمد.

---

## [2026-09-21 · جلسة المعالجة 3] — إغلاق KI-3: ربط منطق الاختيار بالكامل (W-1)

### Added
- **المصفوفة الرباعية (FR-3.1):** تعريف أدوات الخدمات انتقل إلى `universal-content.ts` كبيانات (`hint`/`outputTitle`/`starters`/`affordances` لكل أداة × 7 خدمات × لغتين — قرار A-5). «الأداة القائدة» (آخر اختيار) تقود تلميح الملقّن وشكل المخرج والبدايات السريعة والإتاحات، مع سطر تأكيد حي تحت الحزام.
- **الأسلوب موجّه (FR-3.2):** ثلاث بطاقات أسئلة (النطاق/الجمهور/شكل المخرج) بشرائح نقرة واحدة، ثم بطاقة «معاينة الخطة» بالتكوين الكامل (أدوات × أسلوب × إجابات × إتاحات) وزر «نفّذ الآن» — زر الملقّن يتّخذ «التالي» ثم «نفّذ». الإلغاء في كل خطوة يحفظ المسودة.
- **الإتاحات مربوطة (I-9):** منتقي ملف حقيقي (شريحة مرفق قابلة للإزالة)، تسجيل صوتي حي بمؤقت ونقطة نابضة ومقطع، معيار جودة كقائمة تُسجّل في الخطة والمخرج — كل أداة تفتح مدخلاتها المنطقية فقط.
- **الحفظ (I-6/FR-4.2):** زر «حفظ في المكتبة» في رأس المخرج → توست + دخول العنصر شريط «من مكتبتك» فعليًا (محلي بالجلسة — KI-5).
- شرائح تكوين داخل المخرج تعكس الاختيار (الجدية الأولى لـ FR-3.4) + حالة `clarifying` موثّقة في آلة الحالات §4.

### Fixed
- **أزرار ميتة (FR-3.6):** تبويبات أقسام المخرج (ما فهمته/المسار/الخطوة التالية) تُبدّل محتوى الجسم فعليًا؛ أُزيل زر «افتح المخرج» الذي لم يكن يفعل شيئًا.
- **aria:** بطاقتا الأسلوب تحوّلتا من `tablist`/`tab` بلا tabpanels (نمط مكسور) إلى `role=group` مع `aria-pressed`.
- **FR-2.2:** إزالة آخر أداة مفعّلة أصبحت محجوبة (اختيار واحد على الأقل دائمًا).

### Docs
- `interaction-logic.md`: §2/§3 حالة «منجز» + مخطط موجّه موسّع + آلة حالات بـ `clarifying` + جدول §5 محدّث بالكامل (I-1/I-2/I-9/I-11).
- `requirements-app.md`: FR-3.1/3.2/3.6 منجزة · FR-2.2/4.2 محدّثة · مصفوفة التتبع 38 منجزًا.
- `page-specs.md` C-0: الأقسام 3/5/6/6b/8/9 محدّثة. `known-issues.md` KI-3 مغلق بحلّه الكامل. `roadmap.md` W-1/W-2 منجزة. `AGENT.md` §5/§6/§7/§10.

### Verification
- اختبار تفاعلي كامل للمسارين (موجّه: أسئلة→خطة→تنفيذ→حفظ / سريع: تنفيذ مباشر) بالعربية والإنجليزية.
- رفع ملف CSV عبر المنتقي + تسجيل صوتي (مؤقت حي 0:01 + مقطع) + قائمة معيار الجودة — كلها تظهر في الخطة والمخرج.
- ثيمان × اتجاهان + 390px: صفر تمرير أفقي، فحص VLM للقطات نظيف، لا قصّ نص (تحقق برمجي للـ overflow).
- `npm run build` + `npm run lint` + `tsc --noEmit` نظيفة تمامًا.

---

## [2026-09-21 · جلسة المعالجة 2] — إغلاق KI-2 كتشخيص خاطئ

### Fixed (توثيقي)
- **KI-2 (🟠) مغلق كتشخيص خاطئ:** المسار `/app/models/[modelId]` سليم — الفحص البايت-محمي (Python/os + git index + od) يثبت أن المجلد باسمه الصحيح، والصفحة ترجع 200 لكل النماذج.
- **السبب الجذري للوهم:** مخرجات الطرفية تبتلع التسلسل الحرفي `[m` (تفسيرًا له كبادئة ANSI) فتعرض `[modelId]` كأنه `odelId]` في ls/find/git — وُثّقت قاعدة التحقق البايت-المحمي لمنع تكرار الوهم.

### Docs
- `known-issues.md` KI-2 مغلق بتحقيقه الكامل · `IA §3 صف 24 + §7 قاعدة 4` · `page-specs F-8` · `FR-5.5` · `AGENT.md §7`.

### Verification
- `od -c` على اسم المجلد + `os.path.exists('[modelId]/page.tsx')` = True · HTTP 200 على mdl_clarity / mdl_core / mdl_balanced.

---

## [2026-09-21 · جلسة المعالجة] — إغلاق KI-1: طبقة التشغيل تدخل التنقل

### Fixed
- **KI-1 (🔴 بنيوي) مغلق:** إضافة مجموعة تنقل رابعة **«التشغيل»** في `app-shell.tsx` تضم: التشغيلات (runs) · الاستخدام والتكلفة (usage) · الفوترة (billing) · الفريق والأدوار (team) — كانت مسارات يتيمة كاملة الوظيفة بلا أي مدخل تنقلي.
- `skills`/`tools` استُوثق مدخلهما كرابط سياقي عبر **تبويبات الكتالوج** في صفحة النماذج (مستوفٍ للقاعدة IA §7.2).
- لوحة الأوامر ⌘K تبحث الآن في **19 وجهة** بدل 15 (المصفوفة `allItems` تشمل المجموعة الجديدة).

### Docs
- `information-architecture.md`: §2 أربع مجموعات (19 وجهة) · §3 صفوف 26–32 محدثة · §4 خريطة الترابط بلا «طبقة يتيمة» · §7 قواعد المجموعات الأربع.
- `requirements-app.md`: FR-1.7 → **منجز** · FR-1.2/FR-1.4 محدثة · FR-6.2/6.3/6.4 بلا وصمة «يتيمة».
- `page-specs.md`: قسم G لم يعد «اليتيمة حاليًا». `AGENT.md` §3/§5/§6/§7 محدثة.

### Verification
- فحص 390px: صفر تمرير أفقي؛ درج الهاتف يتمرر داخليًا مع المجموعات الأربع؛ الشريط السفلي الخماسي سليم.
- ⌘K تجد «التشغيلات» بالبحث العربي — والمجموعة كاملة تظهر في سطح المكتب والتابلت (rail/موسّع).
- `npm run build` + `npm run lint` نظيفان قبل الدفعة.

---

## [2026-09-21] — تأسيس منظومة الوثائق · تنظيف المستودع · خط الأساس التوثيقي

### Added
- **منظومة وثائق كاملة** تحت `docs/` بخمسة أقسام مرقمة:
  - `01-product/`: PRD موسّع (رؤية، مبادئ غير قابلة للتفاوض، مؤشرات نجاح) + متطلبات تطبيق مرقّمة (FR/NFR قابلة للاختبار).
  - `02-experience/`: هندسة المعلومات (جدول 34 مسارًا + خريطة الترابط) + منطق التفاعل (مصفوفة «إذا ضغط كذا» + آلات الحالات) + مسارات المستخدم (F-0 إلى F-6) + مواصفات كل شاشة.
  - `03-design/`: نظام التصميم v2 «النول» (توكنز، طباعة عربية، قوانين إيقاع، ممنوعات).
  - `04-engineering/`: البنية التقنية (طبقات واتجاه اعتماد) + نموذج البيانات (ERD وعقود Zod).
  - `05-process/`: العيوب الموثقة (KI-1 إلى KI-6) + خارطة الطريق (W-1 إلى W-5).
- **ملفات التعليمات الحاكمة في الجذر:** `AGENT.md` (مصدر الحقيقة للتسليم بين الجلسات) · `AGENT-GUIDE.md` (القواعد الحمراء العشر) · `ONBOARDING.md` (دليل الجلسة الجديدة) · `worklog.md` (سجل العمل الحي).
- **المخططات المدمجة** في الوثائق (Mermaid — تُعرض مباشرة على GitHub): خريطة الترابط، التكوين الثلاثي، أسلوب التنفيذ، آلة حالات الجلسة، آلة حالات التشغيل، ERD، مسار الموافقات.

### Changed
- `README.md` أعيدت كتابته كاملة ليعكس **الوضع الحالي للمشروع** (نظرة عامة، بنية، وثائق، تشغيل، نشر، سير عمل).
- نقل `PRD.md` من جذر المستودع إلى `docs/01-product/PRD.md` مع توسيعه (النسخة الجذرية أزيلت — النسخة الموسعة هي المرجع).

### Documented
- **KI-1:** طبقة التشغيل (usage/team/billing/skills/tools/runs) خارج التنقل الجانبي — الصفحات موجودة وعاملة لكن بلا مدخل تنقلي.
- **KI-2:** مجلد مسار النموذج الديناميكي معطوب الاسم (`odelId]` بدل `[modelId]`).
- **KI-3:** أدوات العمل وأسلوب التنفيذ في بوابات الخدمات شكلية — المنطق موصّف كاملًا في `interaction-logic.md` ولم يُنفّذ بعد.
- اصطلاح الهوية `ask` مقابل المسار `/chat` (KI-4) وضعت قواعده.

### Verification
- بناء إنتاجي محلي ناجح بكل المسارات قبل الدفعة.
- النشر التلقائي على Vercel `READY` — الإنتاج يرجع 200 على المسارات الرئيسية.
- فحص عرض Mermaid داخل الوثائق على GitHub (تُعرض بدون أدوات إضافية).
